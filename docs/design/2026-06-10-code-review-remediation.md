# Design Document: Code Review Remediation

**Author:** Scott A. Idler
**Date:** 2026-06-10 (re-verified 2026-08-29)
**Status:** In Review
**Review Passes Completed:** 8/8 (2026-06-10) + 5/5 re-verification (2026-08-29: draft, correctness, clarity, edge cases, excellence)
**Re-verified against:** HEAD `5f127eb`, Cargo.toml `1.4.0`, tags `v1.3.0` and `v1.4.0`
**Modules touched:** cfg, cli/parser, cli/commands, executor, executor/state, ports, makefile, tui, app/main, repo/CI

## Summary

A repo-wide code review (8 parallel reviewers, ~150 deduplicated findings, ~18 confirmed empirically) found 15 critical bugs, ~50 majors, and a long tail of minors/nits. This doc converts every finding into a phased fix plan. Every named finding gets coverage here: nothing is deferred or "tracked separately."

Nothing in this plan has been built. In the 2.5 months since it was written, two smaller docs jumped it in line and shipped (`2026-08-28-boundary-fixes-and-dynamic-foreach`, `2026-08-29-strict-ottofile-schema`), each explicitly claiming a slice of this one. The 2026-08-29 re-verification walked all 93 phase bullets against the running v1.4.0 binary and the current tree.

## How to read this document

- **Implementing a phase?** Go straight to it. Every bullet carries a verdict tag, a re-anchored `file:line`, and where it was reproduced an `Observed` line that is the failing test you write first.
- **Verdict tags:** **SHIPPED** (closed since the draft), **SUPERSEDED** (owned by another doc, annotation verified), **OPEN** (unchanged, still reproduces), **CHANGED** (still real, but this doc's description of it was wrong and is corrected inline), **DROPPED** (premise invalid), **MOVED OUT** (left this doc, see Addendum), **NEW** (added 2026-08-29).
- **Trust the tags over the prose.** Line numbers in bullet text were re-anchored on 2026-08-29 against HEAD `5f127eb`; anything older in this doc is historical narrative, not a pointer.
- **Phase 0 is not optional and not reorderable.** Until it lands, no later phase can be verified, because a green local run and a green CI run are currently different things.

## Re-verification: what 2.5 months changed

**Method.** Six parallel verifiers, one per phase pair, each required to reproduce against `target/debug/otto` built from `5f127eb` or point at unchanged code. Doc line numbers were treated as stale and re-anchored. Doc-internal claims (including this doc's own supersession annotations) were treated as unverified until run.

**Headline: the plan is intact.** Of 93 phase bullets, 3 are closed, 30 changed shape, 1 moved out to its own doc, and 59 are unchanged and still reproducible. 5 new bullets are added below, taking the plan to 98.

| Phase | Bullets | Closed | Changed shape | Moved out | Still open | New |
|-------|---------|--------|---------------|-----------|------------|-----|
| 0 Gates and exposure | 5 | 1 | 2 | 0 | 2 | +2 |
| 1 Silent-success core | 10 | 0 | 4 | 0 | 6 | |
| 2 Conditional-deps and foreach | 8 | 0 | 3 | 0 | 5 | |
| 3 Containment | 8 | 0 | 1 | 0 | 7 | +1 |
| 4 State and DB | 10 | 0 | 1 | 0 | 9 | |
| 5 Upgrade and HTTP | 3 | 0 | 2 | 0 | 1 | +1 |
| 6 cfg correctness | 9 | 1 | 0 | 1 | 7 | |
| 7 Converter truth | 7 | 0 | 2 | 0 | 5 | |
| 8 TUI and CLI surface | 10 | 1 | 4 | 0 | 5 | |
| 9 Repo hygiene | 7 | 0 | 3 | 0 | 4 | |
| 10 Doc truth | 7 | 0 | 5 | 0 | 2 | +1 |
| 11 Test-gap closure | 9 | 0 | 3 | 0 | 6 | |
| **Total** | **93** | **3** | **30** | **1** | **59** | **+5** |

Counting rules, so the table is auditable: rows count **top-level bullets only**. The 94th checkbox in the original draft was the Open Questions placeholder, not a work item. Two *sub-bullets* were dropped as invalid and are not rows of their own (`jobs: 0` validation at config load, inside Phase 1's `-j 0` bullet; and the Phase 10 instruction to correct "cycle detection still works"). "Closed" means SHIPPED (1: Phase 0's clippy) or SUPERSEDED (2: Phase 6 `deny_unknown_fields`, Phase 8 help drift). Phase 11's cfg bullet was tagged SUPERSEDED in an earlier draft and is **not** closed: panel review established that only two of its eight items shipped, so it is split and counted open.

**All five of this doc's own supersession annotations verify TRUE**, three of them top-level bullets and two nested inside batch bullets (Phase 8's `-C=DIR` inside the CLI-rule batch, Phase 3's `$()` inside the env remainder). That is worth stating plainly, because it is the part of the process that worked: when `2026-08-28` and `2026-08-29` de-duplicated against this doc, they took exactly what they said they took, and left exactly what they said they left.

- Phase 6 `deny_unknown_fields` -> shipped `b5a86dc`, and shipped *wider* than the bullet asked: six structs, not five (`ParamSpec` came along), seven pinning tests plus an inventory drift test.
- Phase 8 help drift -> shipped `bb0f8dc`, `global_args()` at `parser.rs:649` feeding all three builders, pinned by `test_help_global_flags_no_drift`.
- Phase 8 `-C=DIR` attached form -> shipped `bb0f8dc`, `main.rs:78`, pinned at `main.rs:320`.
- Phase 3 `$()` paren balancing -> shipped `10eb334`, quote-aware level stack at `env.rs:175-200`, pinned by three cases in `tests/env_command_substitution_test.rs`.
- Phase 11 cfg test bullet -> covered by the same `2026-08-29` work.

**The one bullet that closed on its own:** Phase 0's clippy errors, fixed by `037a4dc`. `cargo clippy --all-targets --all-features -- -D warnings` on 1.96.0 is now clean.

**The counts the original doc quoted are all wrong now**, and the corrected figures are used throughout below:

| Claim in the 06-10 draft | Measured 2026-08-29 |
|---|---|
| 590 tests pass locally | 748 pass locally (535 lib + 12 main + 201 integration) |
| 15 log statements, one DEBUG | 48 log calls, 13 of them `debug!` |
| across ~1066 functions | across 638 production functions (excluding `#[cfg(test)]` regions) |
| parser.rs 3457 lines | 4091 |
| scheduler.rs 2256 lines | 2684 |
| three files over the 1500 cap | four: `cfg/task.rs` reached 1576 |
| ~50 underscore-prefixed bindings | 55, across 15 files |
| 8 `process::exit` calls in parser.rs | 12 |

### The gates got worse, not better

This doc's Phase 0 exists because main CI had been red for 15 consecutive runs over 3+ months. It is still red, and v1.4.0 made it redder.

**Run `33284438194`, HEAD `5f127eb`, conclusion `failure`, `530 passed; 5 failed`.** (Source, since this is not a code-local claim: `gh run view 33284438194 --json conclusion,headSha` -> `{"conclusion":"failure","headSha":"5f127eb78b4911c0cfc5e152a04bac6d4d045c4b"}`; the paired Release run is `33284439253`, conclusion `success`, same SHA.) Four of the five are the same environment-coupled clean tests this doc named on 2026-06-10:

- `cli::commands::clean::tests::test_execute_with_database_actual_delete`
- `cli::commands::clean::tests::test_execute_with_database_keep_failed_longer`
- `cli::commands::clean::tests::test_execute_with_database_keep_last`
- `cli::commands::clean::tests::test_execute_with_database_project_filter`

The fifth is new, and it is the same defect class arriving by a different door:

- `cli::parser::tests::test_help_global_flags_no_drift`

That test shipped in `5f127eb` (v1.4.0) as the anti-drift guard for the help-flag single-sourcing. It pins `[default: 32]` in the expected help text, which is the developing machine's `num_cpus`. It passes locally and fails on every CI runner with a different core count. A guard written to stop drift now cannot run on the surface it guards.

The practical consequence, and the reason Phase 0 stays first and alone: **local test runs and CI disagree, and the local run is the misleading one.** Two independent verifiers ran the suite on this machine and got 748 passing, exit 0, because this machine has a populated `~/.otto` and 32 cores. Neither condition holds on a runner.

**And the red never stopped a release.** Each commit produces two workflow runs, and they disagree by design:

| SHA | `CI` | `Release` | What shipped |
|-----|------|-----------|--------------|
| `5f127eb` (v1.4.0) | **failure** | success | binaries, GitHub release, Docker image |
| `5640628` | **failure** | success | (v1.3.0 tranche) |
| `10e9cac` | **failure** | success | |
| `2f42875` | **failure** | success | |

`Release` fires on `push: tags: v*` and never consults `CI`. So `bump` publishes regardless. That is not a gate that is being ignored; it is the absence of a gate, and it is why three months of red became six without anyone having to decide to ship anyway. Phase 0 closes it.

### What shipped underneath this plan

v1.3.0 and v1.4.0 changed the tree this plan targets. Three effects, all reflected in the phases below.

1. **Some bullets got partially fixed by work aimed elsewhere.** Cycles and conditional skips are now *visible* because `121f384` made skip reasons print, though neither bug is closed. Serial foreach no longer uses chain edges at all.
2. **Some bullets got harder.** The kebab-case flip now has to invert a shipped test and update a published 44-key reference. `process::exit` grew from 8 sites to 12 because `--tasks` and `foreach: command:` added four more.
3. **New surface arrived carrying the old defect classes.** `foreach: command:` and `choices-command` are genuinely good features that route freshly-executed command output into the same unescaped script generator this doc's Phase 3 was written to close.

### New items added 2026-08-29

Five, each landing in an existing phase. The three that would change ship order:

- **`otto Upgrade` cannot work at all** (new, Phase 5). `download_with_progress` binds `let temp_dir = tempfile::tempdir()?` at `upgrade.rs:335`, writes the tarball inside it, and returns `Ok(file_path)` at `:352`. `TempDir` drops at function exit and removes the directory, so `install_from_archive` opens a path that no longer exists (`:432`). The ETXTBSY bug this doc found at `:464` is real but unreachable: the archive is gone four lines earlier. Verified by binding the same types against this repo's own `tempfile` rlib (`inside f: exists=true` / `after return: exists=false`).
- **Nothing gates a release on the test suite** (new, Phase 0). `Release` fires on tag push and never consults `CI`, so every `bump` since 2026-03-02 has published binaries and a Docker image off a red suite. This is the root cause behind the finding this doc's Phase 0 originally described as "the repo's own gates lie": the gate does not exist rather than being ignored.
- **`foreach: command:` and `choices-command` output reaches the shell raw** (new, Phase 3). A `foreach` item `a"b` emits `export OTTO_FOREACH_ITEM="a"b"` and a bash syntax error. An item `../../../ESCAPED` created a directory outside the run's `tasks/` tree. An item containing `${IFS}` aborts the task env, i.e. command output is re-expanded as syntax. A `choices-command` value `a";touch${IFS}/tmp/OTTO_PWNED_CHOICES;echo"b`, a legitimate member of the command's own output, created the file. The only sanitizing anywhere is space-to-underscore on the *identifier* at `task.rs:209`; the *value* reaches `action.rs:205`/`:254` untouched.

The other two are recorded in place, tagged **NEW**: the CI-only test failure `test_help_global_flags_no_drift` (Phase 0) and the six inert-but-documented `otto:` keys (Phase 10). Two further corrections ride inside existing bullets rather than as new rows: `--no-prefix` not suppressing scheduler status lines (Phase 8) and the upgrade release-note obligation (Rollout note 5).

Separately from new bullets, **30 existing bullets had their description, site, or mechanism corrected**. Those are not new work; they are the same work, described accurately. Each is tagged **CHANGED** with the correction called out inline, because a bullet that names the wrong file or the wrong mechanism is what makes an implementation audit produce false deviations.

## Problem Statement

### Background

The 2026-06-10 review examined all of src/ (cfg, cli, executor, executor/state, ports, makefile, tui, app/main/utils), the test suite, docs/, CI, and repo hygiene, validated against the user rules (logging, CLI conventions, naming, test placement, no-dead-code) and the four docs/design/ documents. Empirical confirmations include: shell injection via env values (a probe value created a file on disk), bare `otto` running the Clean builtin instead of user tasks, typo'd task names exiting 0, dependency cycles exiting 0 with zero output, `--keep-last` deleting the newest runs in filesystem mode, `otto clean` following a symlink and deleting a directory outside `~/.otto`, the v1-to-v2 migration crash window bricking the DB ("duplicate column name"), `nargs: "0:5"` panicking the config loader, multibyte-whitespace dedent panicking, `$(shell ...)` and `$(VAR)` surviving Makefile conversion and breaking at runtime, invalid UTF-8 truncating captured task output while the task "succeeds", six different cache filenames for the same script across six runs, a spawn failure being reported as a task named "such" and then hanging the scheduler, `otto Upgrade` dying on ETXTBSY, `-j 0` hanging forever, and main-branch CI red for 15 consecutive runs over 3+ months.

**2026-08-29: every one of those empirical confirmations was re-run and every one still reproduces**, with three descriptions corrected (cycles now print skip lines but still exit 0; `-j 0` is a 99.3%-CPU hot spin rather than a block; `otto Upgrade` fails before ETXTBSY is reachable).

Positive findings worth recording: drain-on-failure works exactly as the conditional-deps design promises (failing task drains parallel work, `when: failure` fixer and `when: always` cleanup both fire, exit stays 1); no SQL injection exists anywhere (every user value is bound via `params![]`); the concurrent stdout/stderr drain design avoids the classic pipe-buffer deadlock (verified at 4 MB of output); the ports layer is honest DI; cfg/edge.rs is a model file; the ParamSpec round-trip shipped exactly as its design doc records; `apply_cwd_flag` and log rotation in main.rs are careful and well tested; auto-prune runs even on failed runs with the rationale recorded; install.sh verifies checksums; version discipline (Cargo.toml == latest tag, GIT_DESCRIBE) is correct; and `.otto.yml` genuinely dogfoods the flagship features.

**2026-08-29 additions to the positive list.** The v1.3.0 features were probed for the defect classes in this doc and came back clean on every axis except escaping: `foreach: command:` and `choices-command` both fail loudly with exit 1 rather than silently producing an empty list; neither executes at `--help` time (`other --help` renders `[dynamic choices: <command>]` without running it, and an execution counter confirmed zero extra runs); `tty: true` serializes correctly at `-j 1` and at default `-j`; `--tasks` honors `-o` and emits JSON when piped. `DynamicResolver` memoizes per invocation in a `RefCell<HashMap<..>>` (`resolver.rs:44-54`) and deliberately does not cache failures.

### Problem

Six systemic defect classes, plus per-module bugs. **All six still hold on 2026-08-29**; class 6 has gotten measurably worse.

1. **Silent success on broken input, the worst failure mode a task runner can have.** A typo'd task name exits 0 having run nothing; a dependency cycle exits 0; a top-level `task:` typo used to parse to zero tasks (**closed by v1.4.0**); bare `otto` with default `tasks: ["*"]` expands the injected builtins and runs `Clean` instead of the user's tasks; `otto: tasks: []` plus an explicit task request prints nothing and exits 0; the Makefile converter silently corrupts continuations, pattern rules, `export`, and target-specific variables into plausible-looking garbage; foreach with up-to-date subtasks silently kills everything downstream. **New instance:** six `otto:` keys (`jobs`, `home`, `verbosity`, `name`, `version`, `about`) are accepted, ignored, documented as working behavior in `docs/commands/ottofile-reference.md:39-42`, and now *guaranteed* to parse by `deny_unknown_fields`. Strict parsing made accepted-and-ignored keys look official.
2. **Two backends, two behaviors.** The DB and filesystem-fallback paths of `clean` disagree: `--keep-last` is inverted in fs mode, `--keep-failed` is silently ignored, `--project-filter` is substring-match in one and exact-hash in the other, and the DB path reconstructs run directories as `otto-<hash>` while workspaces are actually `<name>-<hash>`. **Measured against the live `~/.otto`: 222 project directories, of which 2 begin with `otto-`.** The same `otto-` prefix assumption appears a third time in `pruning.rs:90`, so cache pruning also sees 2 of 222. TUI and terminal paths diverge. Mem and Real port impls diverge on UTF-8, missing-file removal, and canonicalize, and duplicate the retention policy line-for-line.
3. **State and persistence integrity.** The v1-to-v2 migration runs ALTER and set_version as separate non-transactional steps; a crash between them bricks the DB forever, and `StateManager::try_new()` is `.ok()` with zero logging. `runs.timestamp` carries a global UNIQUE in seconds. `record_run_complete_in_db` has zero production callers: **its only two references in the entire tree are its definition and its own unit test**, so every run is perpetually "running". The recorded exit code is always 1. No busy_timeout, and zero `spawn_blocking` anywhere. Plain `fs::write` plus a write-only-if-absent script cache means a torn write permanently poisons an executable script. The script hash itself is nondeterministic.
4. **Strings where types belong.** The scheduler extracts the failed task's name by parsing the error string (`split_whitespace().nth(1)`, so a spawn failure becomes task "such" and the run hangs); the exit code travels through `{:?}` formatting and back; early `?` exits inside spawned task bodies bypass the completion channel and JoinHandles are never awaited; `env_to_json` corrupts multiline values; eyre chains are flattened in main.rs. **STAYS: NOT covered by `2026-08-29-strict-ottofile-schema.md`** (named a Non-Goal there; it remains remediation scope).
5. **Containment and injection.** Env values and CLI params are templated raw into generated bash/python scripts; command-substitution output is re-expanded as variable references; a bare `$VAR` is silently left unresolved when `${VAR}` appears in the same string; `clean` follows symlinks under `~/.otto` and `remove_dir_all`s targets outside it; `otto Upgrade` does no integrity verification and cannot complete an install anyway. **The class grew in v1.3.0**: `foreach: command:` and `choices-command` add two fresh sources of externally-shaped text feeding the same unescaped generator, plus a path-traversal vector through foreach item names.
6. **The repo's own gates lie, and one of them does not exist.** Main-branch `CI` is still red, now at 5 failures rather than 4, one of them shipped by v1.4.0 itself. `Release` never consults `CI`, so the red has never blocked a publish. The three steps of the CI `test` job run sequentially in one job, so the test failure means `cargo fmt --check` and `cargo clippy` have not actually executed on main in months, and any drift they would catch is invisible rather than merely tolerated. `Cargo.lock` is still gitignored for a binary crate whose pre-commit hooks run `--locked`. The GH workflow's clippy is still bare `cargo clippy -- -D warnings` against `--all-targets --all-features` everywhere else, and its steps are sequential fail-fast so the test failure still hides fmt and clippy. A phantom tracing/tracing-subscriber logging stack is still declared and never referenced. 13 `debug!` calls cover 638 production functions against an always-on entry-DEBUG rule, with `executor/action.rs`, `executor/state/manager.rs`, `makefile/parser.rs`, and `ports/http.rs` at zero and `scheduler.rs` holding 33 of the 48 total. docs/ still mixes evergreen references with ~20 stale one-off plans including two that document a removed nom parser, and v1.4.0 added two more files to that directory rather than pruning it. Real Tatari service Makefiles are still committed in this repo, which `gh repo view` confirms is `PUBLIC`.

### Goals

- Fix all 15 criticals with regression tests in the same change.
- Eliminate the six systemic classes, not just their instances.
- Make every doc (design docs, README, converter doc, code comments, `.cursor/rules`) tell the truth: implement the claim or amend the doc, never leave the contradiction.
- Bring rule-drift up to the user's own rules (CLI case-insensitivity, no RUST_LOG, no `dirs::data_local_dir()`, tests in sibling files, no `#[allow(dead_code)]`, entry-DEBUG logging, file-size caps).
- Get and keep the gates green: CI, clippy, fmt, and the dogfooded `otto ci`, **on a runner and not merely on the developing machine**.
- Close every test gap the review identified.

### Non-Goals

- New features beyond what the docs already promise (no new builtins, no `--skip` flag, no nom parser revival).
- Re-litigating settled architecture (clap two-pass parsing, capitalized builtins, file-based data passing, ports DI: all deliberate and documented).
- Performance work beyond the specific findings, with one exception promoted to scope: the per-call `Regex::new` in env.rs is quadratic in chain depth (measured 12.1s at 100-deep, 22.3s at 150, 45.0s at 200), which is a correctness-adjacent hazard rather than a tuning opportunity.
- mod.rs to 2018-style module migration (the rule itself says dedicated tree-wide pass, never mixed into feature work).
- **The kebab-case key flip** (moved out 2026-08-29, see Addendum). It needs aliases, a deprecation window, an inverted test, and a reference-doc update, which `2026-08-29` already said makes it its own doc.
- **`--plan`, whole-run and per-task timeouts, lazy env evaluation, and the staged parser pipeline.** These belong to the sibling doc, which is missing (see below).

### The missing sibling doc

Both `2026-08-28` and `2026-08-29` sequence work against `docs/design/2026-06-10-architecture-product-completion.md`, which owns `--plan --format json`, `--list-tasks`, timeouts, and moving `$()` execution out of load time ("stage 2: NO subprocess execution"). **That file does not exist.** It is not in the working tree, not in git history (`git log --all --diff-filter=A` finds nothing), and not in the stash. It was an untracked draft and it is gone.

Its scope is partially recoverable from the "Product utility gaps" and "Architectural liabilities" sections below, which describe the same surface. Recorded here so the dangling references in two shipped docs have an answer, and so the work is not silently lost a second time. Re-authoring it is out of scope for this doc.

## Architecture and Product Assessment

The phased plan below is tactical. This section is the altitude view: whether the architecture is the right shape, and whether otto-the-product is worth the investment the plan represents. **Re-checked 2026-08-29; the assessment stands, with the drift noted inline.**

### What otto is

A declarative YAML task runner positioned as a make replacement: DAG dependencies (`before`/`after`), parallel-by-default execution (`-j`), conditional edges (`when: success|failure|always` plus `on-failure:` sugar) with drain-on-failure, foreach fan-out (glob/items/range/**command**, serial or parallel, with aggregating virtual parents), file dependencies (`input`/`output` up-to-date skipping), per-task typed CLI params that become real clap subcommands with help/choices/**dynamic choices**/defaults, env evaluation with command substitution, file-based data passing between tasks, sqlite-backed run history with History/Stats/Clean builtins, a TUI dashboard, a Makefile converter, self-upgrade, **a machine-readable `--tasks` surface, `tty: true` for tasks needing the terminal, and `--no-prefix`**: shipped as a single static binary with install.sh, Docker, and a GitHub Action.

### Positioning

Otto sits in a real gap between the command launchers (just, go-task) and the artifact-correct build systems (bazel, earthly). The thesis is effectively **CI semantics on your laptop**: a DAG with conditional flow, observable history, and per-task CLI ergonomics. Four features are genuinely distinctive and no near-peer has the combination:

1. **Params as real subcommands.**
2. **Multiple tasks per invocation, each with its own args.**
3. **Conditional dependencies with drain-on-failure.**
4. **Run history and stats.**

A fifth is now arguably in that set: **dynamic fan-out and dynamic choices from a command**, which is what made otto a plausible replacement for a hand-rolled generator in a consumer repo. It is also the feature that most needs Phase 3's escaping work before it can be recommended.

One deliberate scope boundary worth stating: otto is task-oriented, not artifact-oriented. File deps are a freshness check, not make's incremental-build model. That is the right call for the niche, but the Makefile converter implies a parity the model does not have, which is part of why Phase 7 makes it diagnose loudly what it cannot express.

### Architectural strengths

- **The layering direction is right.** cfg -> cli -> executor -> state, with data flowing one way.
- **The ports layer is honest hexagonal architecture**, with one correction: `ports/http.rs` is now entirely dead (see Phase 5/9). The fs and db ports remain genuinely exercised.
- **The execution core has the right shape**: an event-driven scheduler over bounded channels, semaphore-bounded parallelism, concurrent pipe drains, and a single `classify_edge` source of truth. v1.3.0's serial-foreach redesign (ordering as a scheduler property via `SerialGroups`, `scheduler.rs:208-278`, replacing chain `before:` edges) is a strict improvement to that shape and vindicates it.
- **Style-preserving round-trip config serialization** is unusually sophisticated for a tool this size.
- **Workspace-per-run plus a content-addressed script cache** is a good persistence design once Phase 3 makes the hashing deterministic and the writes atomic.
- **The distribution posture matches the niche**, with the caveat that the self-upgrade leg of it is currently non-functional.
- **The strict-schema work is the model for the rest of this plan.** One reproduced defect, one small doc, fail-closed by construction, error messages naming field and path and line, a published key reference, and a drift test that fails when the schema and the reference disagree. That is what "eliminate the class, not the instance" looks like when it lands.

### Architectural liabilities

These are structural, beyond the per-site bugs the phases fix.

1. **parser.rs is a god module,** and it grew: 3457 lines at review time, **4091 today**. Phase 9 decomposes it, but the deeper fix is a staged compilation pipeline: parse args -> load config -> expand/propagate -> plan, with typed artifacts between stages. That was the missing sibling doc's core proposal.
2. **Builtins are phantom tasks,** injected into the user's namespace then filtered or intercepted at three layers. Root cause of two criticals. Now also the reason `Convert` and `Upgrade` are unreachable behind any global flag while `Clean`/`Graph`/`History`/`Stats` work: the difference is simply which ones got hand-written handlers in `app.rs:296-313`.
3. **Token-equality partitioning is inherently ambiguous.** Phase 8's lookahead fixes the common case; the `=`-form escape hatch must stay documented.
4. **Presentation forks above the scheduler instead of below it.** Partially improved: `is_virtual_parent` no longer drifts, because both paths now route through `impl From<parser::Task> for Task` (`executor/task.rs:249`, `121f384`). The ~40 lines of duplicated setup and the builtin divergence remain.
5. **Three "Task" types with lossy conversions** (`cfg::TaskSpec`, `parser::Task`, `executor::Task`).
6. **Stringly-typed seams.** eyre string errors at every module boundary are why the scheduler parses task names out of error messages.
7. **Config loading executes code.** `$()` runs at load time. v1.3.0 was careful here (neither `foreach: command:` nor `choices-command` runs at `--help` time, verified), which makes the *existing* eager `$()` evaluation the odd one out. Document the posture (Phase 10); lazy evaluation is the structural fix.
8. **The state layer mixes storage with disk lifecycle.** `StateManager` performing `remove_dir_all` is a layering violation that helped the wrong-dirname bug hide, and it means the symlink-deletion hole has to be patched in two places.

### Product utility gaps

Not bugs: missing or questionable surface, judged against the "CI semantics locally" thesis. **Re-checked 2026-08-29.**

- **No plan/dry-run output for tasks.** `--tasks` (v1.3.0) delivers the machine-readable task list, which was the near-term half. "What would run, in what order, and why" is still absent. (`dry_run` in app.rs is the Clean builtin's flag, not a scheduler plan.) Strongest candidate for the next feature doc, and it was the missing sibling doc's headline.
- **No timeouts anywhere.** Still zero: no `timeout:` task key, no global ceiling. Two `.cursor/rules` files document a `timeout:` key that has never existed.
- **No shell completions.** `clap_complete` is not a dependency. No watch mode.
- **The README improved but is still thin.** 121 lines now, up from 62. The false build-time version-mismatch claim is still there at `README.md:121` against a 23-line `build.rs` that only sets `GIT_DESCRIBE`.
- **The TUI is the least essential surface and the least finished.** Unchanged: dead Ctrl+C, ignored builtins, incoherent scrolling, width-0 hang.
- **The Makefile converter's strategic value is onboarding, and in its current state it harms onboarding.** `src/makefile/` has gained exactly one line since 2025-11-06.
- **The differentiating feature is still the least trustworthy one.** Run history/stats is otto's unique selling point, and every defect this doc found there is untouched: runs never marked complete, exit codes always 1, concurrent runs vanishing, clean deleting the wrong things or nothing. Phase 4 is product-critical, not hygiene.

### Verdict

The architecture is the right shape with two structural exceptions (the parser god-module and builtins-as-phantom-tasks), and the execution core's design decisions are sound and in several places better than the tool's weight class. The product occupies a real niche with four, arguably five, genuinely distinctive features. The gap is trust: the silent-success class lands precisely on the features that differentiate otto, so the tool is most dangerous exactly where it is most interesting.

**2026-08-29 amendment.** Two and a half months of evidence sharpen this. v1.3.0 and v1.4.0 demonstrate the team can ship small, reproduced, fail-closed fixes with drift tests, which is the exact competence this plan needs. They also demonstrate the failure mode this plan exists to stop: shipping onto an unverified base. v1.4.0 added a test that cannot pass on CI, documented six keys that do nothing, and made a published reference authoritative for a schema whose retention keys are still inconsistent with the rest of the format. None of that is careless work; it is what happens when the gates do not run. Phase 0 is not hygiene, it is the precondition for trusting anything else in this document.

## Proposed Solution

### Overview

Twelve phases ordered by severity and dependency: gates and exposure first (a red CI means nothing else can land verified; the Tatari fixtures are a live exposure), then the silent-success criticals in the parse/schedule core, conditional-deps semantics, injection/containment, state integrity, upgrade safety, cfg correctness, Makefile converter truth, TUI/CLI surface, repo hygiene, doc truth, and test-gap closure. Every fix lands with its regression test in the same phase. `otto ci` gates every phase (and after Phase 0, actually can).

### Architecture

No new modules. The structural changes, all inside existing files or extracted from them:

- The scheduler's completion channel payload becomes typed: `Result<String, TaskFailure { name: String, error: eyre::Report, exit_code: Option<i32> }>`, the task name and exit code carried structurally on both arms, never parsed out of strings. Every exit path of the spawned task body sends exactly one message, and handles are reaped via JoinSet so panics are observed.
- `TaskStatus::Skipped` gains provenance (`SkipKind::UpToDate | SkipKind::Unreachable | SkipKind::SerialPredecessor`), and `skipped_set` widens from `HashSet<String>` (`scheduler.rs:613`) to `HashMap<String, SkipKind>` to carry it so foreach aggregation and `when:` classification can distinguish "skipped because cached" (success-like) from "skipped because a prerequisite failed". **Re-anchored 2026-08-29:** the aggregation override at `scheduler.rs:729-730` reads status only, which is precisely why it cannot tell those apart. That override is the fix site, not `classify_edge`.
- **`classify_edge` stops short-circuiting Skipped sources** (`scheduler.rs:176-178`), and instead resolves them against the edge's `when` using the provenance table below. **The code already contains both semantics and they disagree:** the executor's dependency double-check at `scheduler.rs:955-963` already says `(When::Always, Some(TaskStatus::Skipped)) => true` under the comment "when: always is satisfied by any terminal state", while `classify_edge` returns `Unreachable` for the same edge before it ever matches on `when`. Two gates on the same edge, opposite answers. This is a reconciliation, not a new policy.

  **The `Skipped` provenance contract** (this is what `SkipKind` is for; today the code cannot express it, which is why a one-line inversion is not sufficient):

  | Source `SkipKind` | `when: success` | `when: failure` | `when: always` |
  |---|---|---|---|
  | `UpToDate` (file deps satisfied) | Satisfied | Unreachable | Satisfied |
  | `SerialPredecessor` (earlier group member failed/skipped) | Unreachable | Unreachable | Satisfied |
  | `Unreachable` (a prerequisite failed or was skipped) | Unreachable | Unreachable | Satisfied |

  Rationale per column. `UpToDate` is success-like, so it satisfies `success`; this is what the current double-check's `(When::Success, Skipped) => true` at `:957` is groping for without being able to tell the kinds apart. `always` is satisfied by every terminal state, which is the point of `always` and what makes cleanup reliable.

  **The whole `failure` column is Unreachable, deliberately.** An earlier draft of this table marked `SerialPredecessor` and `Unreachable` as satisfying `when: failure`, which would have redefined `when: failure` from "the source task's action failed" to "something upstream prevented this node running". That is a different language and it was never sanctioned. Four independent statements of the opposite invariant, one of them in the code itself:

  - `2026-05-24-conditional-task-dependencies.md:55` (Non-Goals): "`when: failure` fires only on a non-zero exit from the task's action script."
  - Same doc `:86`: "don't run the fixer if the foreach group never actually started."
  - Same doc `:892`: the resolved `--skip` question.
  - **`scheduler.rs:770-773`, in-code:** a skipped virtual parent enters `skipped_set` "so that downstream `when: failure` edges to this parent become Unreachable rather than Pending forever."

  `tests/foreach_aggregation_test.rs:203-204` is titled "A downstream `when: failure` does NOT fire (Skipped is not Failure)" and `:260-261` asserts `fixer == Skipped`.

  **Two further reasons, which are what actually settle it.** First, **no Phase 2 success criterion needs those cells**: the serial-foreach fixer is delivered by the aggregation (`scheduler.rs:727-731` checks `any(Failed)` before `any(Skipped)`, so the fixer's failure edge sees a Failed parent, never a Skipped one), the warm-cache criterion uses the `success` column, the cleanup criterion uses `always`, and the paired-edge criterion is unrelated. The cells were unrequired by the phase that introduced them. Second, they carry real blast radius: if `a` fails, then `b` (holding `when: success` on `a`) becomes `Skipped(Unreachable)`, and every `when: failure` handler attached to `b` would fire. **One failure would fan failure-handlers across the entire downstream cone.**

  Caught in panel round 2. **`when: failure` continues to mean the source task ran and its action exited non-zero.**

  This is also why the serial-foreach bullet still closes: a serial group whose member *failed* aggregates the parent to `Failed`, not `Skipped`, because the aggregation checks `any(Failed)` before `any(Skipped)`. The fixer fires off a Failed parent. It never needed `failure` to accept a skipped source.

  **This table is new design, not a re-anchoring, and is called out as such.** Skip provenance does not exist today: it is `skip_reasons: HashMap<String, String>` (`scheduler.rs:342`, written at `:448`), free text intended for display, with no type distinction any scheduling decision can read. `SkipKind` introduces that distinction. Because six of the nine cells are non-obvious, the contract ships with a **table-driven test asserting all nine cells explicitly** (`classify_edge_skip_provenance_matrix`), not merely the cases the fixes happen to exercise. That test is the artifact that makes this table enforceable rather than aspirational, and it is named in Phase 2's success criteria and Phase 11's inventory.
- Script generation stops templating values: env vars, params, foreach items, and dynamic choice values pass through the process environment (the scheduler already calls `.envs()` at `scheduler.rs:1067`), or where materialization is unavoidable, values are escaped with the same single-quote escaping `otto_serialize_output` already uses.
- `TaskSpecs`/`ParamSpecs` move from HashMap to IndexMap, making serialization deterministic and author-ordered.
- The `FileSystem` port gains atomic write (temp file in the target's own directory + rename); the script cache and all persisted writes route through it.
- A converter diagnostics system: `MakefileParser` tracks line numbers and accumulates warnings; `--strict` (currently a dead flag) promotes warnings to errors.
- **A single `run_root()` helper** replaces the three independent `otto-<hash>` prefix assumptions (`manager.rs:880`, `clean.rs:282`, `pruning.rs:90`) and the three home-dir mechanisms.

### Data Model

- `runs.timestamp` loses its global UNIQUE; run identity keys off `runs.id` (`last_insert_rowid`), which `record_run_complete`/`delete_run` take instead of a wall-clock timestamp. The run's actual directory name (`<project-name>-<hash>`) is recorded at run start so deletion stops reconstructing it wrongly.
- Each migration step and its `set_version` run inside one transaction, and the v1-to-v2 ALTER becomes idempotent (pragma_table_info check before ALTER).
- The DB connection sets `busy_timeout` (named const) and `synchronous=NORMAL` under WAL at open.
- Recorded task rows carry the real exit code (`status.code()` captured where the error is built).

### API Design

- Unknown task names error with near-match suggestions; cycles error at scheduler init (reusing the daggy `add_edge` WouldCycle detection, which **works today and is simply never wired to the execution path**: `otto Graph` on a cyclic file already exits 1 with `WouldCycle`); `tasks: []` with explicit task args runs them.
- Builtin meta-tasks are excluded from `*` default-task expansion, and builtins are routed in main by scanning past global flags.
- `parser::parse` honors its `Result` contract; the remaining `get_matches_from` at `parser.rs:1028` and all 12 `process::exit` calls move to main.
- `nargs` gets wired to clap `num_args` (space-separated per the CLI rule, never `value_delimiter`); `dest` and `constant` are deleted until a consumer exists.
- `history --status` becomes a `ValueEnum` with `ignore_case = true`; choices-bearing params get `ignore_case(true)`.
- `--log-level` flag replaces `RUST_LOG`; the log path derives from an `xdg_data_dir()` helper instead of the banned `dirs::data_local_dir()`.
- **The six inert `otto:` keys** (`jobs`, `home`, `verbosity`, `name`, `version`, `about`) are either wired to real behavior or deleted from the schema and from `docs/commands/ottofile-reference.md`. Accepted-and-ignored-but-documented is the worst of the three states.

### Implementation Plan

Verdict tags, applied 2026-08-29: **SHIPPED** (closed since the draft), **SUPERSEDED** (owned by another doc, verified true), **OPEN** (unchanged, still reproduces), **CHANGED** (still real, description corrected), **DROPPED** (premise invalid), **NEW** (added 2026-08-29).

#### Phase 0: Green gates and exposure removal
**Model:** sonnet

- [x] **SHIPPED** (`037a4dc`) Fix the 8 `unnecessary_sort_by` clippy errors. All sites now `sort_by_key` (`ports/db.rs:266,277,287,319,380,409,441,462`, `cli/commands/upgrade.rs:425`). Verified: `cargo clippy --all-targets --all-features -- -D warnings` on 1.96.0 finishes with zero warnings.
- [x] **OPEN** Fix the 4 environment-coupled clean tests. `execute_with_store` still calls `get_otto_home()` (`$HOME/.otto`) and early-returns before touching the injected store: `clean.rs:67-72`, helper at `:397-400`. Reproduced with `HOME=$TMPDIR/fakehome`: `No ~/.otto directory found`, `2 passed; 4 failed`. Same four tests are in today's red CI run. Fix unchanged: inject otto-home (env override or constructor param); skip the existence check when a store is injected.
- [x] **NEW** Fix `test_help_global_flags_no_drift` (`parser.rs:3986-4004`, shipped `5f127eb`). It pins `[default: 32]`, the developing machine's `num_cpus`, so it is green locally and red on every runner. It is the 5th failure in run `33284438194`. Fix: normalize the jobs default out of the expected text, or pin against a fixed `num_cpus` injected for the test. The anti-drift intent is right and must survive the fix.
- [x] **CHANGED** Remove the Tatari service Makefiles (`makefiles/{auth-svc,media-planning-service,devs,pre-commit-hooks}/`) and `docs/list-of-all-makefiles` from this repo, and replace fixtures with synthetic Makefiles (coordinate with Phase 7's new negative fixtures). **Scope corrected: deleting the files does not un-publish them.** They entered history in `3822770` on 2025-12-08 (`git log --reverse --format='%h %ad' --date=short -- makefiles/`) and `gh repo view otto-rs/otto --json visibility,forkCount,stargazerCount,createdAt` returns `{"createdAt":"2022-02-28T01:42:52Z","forkCount":0,"stargazerCount":0,"visibility":"PUBLIC"}`, so they have been publicly fetchable for the whole period. `docs/list-of-all-makefiles` is 6964 lines whose section headers are absolute developer paths of the form `/home/saidler/slam/tatari-tv/<repo>/Makefile`, so it discloses internal repo names and a local directory layout, not just Makefile bodies.
  **Decision (2026-08-29): delete now, and treat history rewrite as a separate explicit act.** Deleting stops further exposure immediately and costs nothing. Rewriting history on a public repo is irreversible for anyone who has cloned, so it does not ride along inside a remediation phase. The mitigating facts are recorded so the call can be made on evidence: `forkCount: 0`, `stargazerCount: 0`. **Revisit condition:** any decision to rewrite is taken deliberately, announced, and executed as its own change, not as part of Phase 0.
  **Answering the review directly, because the distinction matters:** deletion does **not** resolve this exposure, it stops it accruing. There is no rotation equivalent for published source; unlike a credential, the disclosed artifact cannot be invalidated after the fact. The only irreversible mitigations are a history rewrite plus a GitHub support request to purge cached views, and even that cannot reach anything already cloned or mirrored. It is not made a hard Phase 0 requirement for one reason only: force-pushing rewritten history on a public repository is itself irreversible and breaks every existing clone, so it is an owner decision made on the recorded facts, not a step an implementer performs while fixing tests. **What Phase 0 does guarantee:** the exposure stops growing, and the decision is on the record instead of implicit.
- [x] **OPEN** Commit `Cargo.lock` (binary crate); drop it from `.gitignore:7`. `.pre-commit-config.yaml` runs `cargo test --locked` against a lockfile git does not track.
- [x] **CHANGED** Align the GH workflow's clippy invocation. `.github/workflows/ci.yml:37` is still bare `cargo clippy -- -D warnings` vs `--all-targets --all-features` in `.otto.yml:25` and pre-commit. **Correction:** the steps *are* already separate, but they are three sequential steps of one `test` job (`ci.yml:30,33,36`) with no `if: always()`, so today's test failure means fmt and clippy never execute at all. Fix: make the steps independent, align the invocation, and have CI run `otto ci` so the repo dogfoods what it recommends to others.
- [x] **NEW** Gate `Release` on `CI`. `.github/workflows/release-and-publish.yml` triggers on `push: tags: v*` and its only `needs:` are internal (`:146` `[build-linux, build-macos]`, `:162` `build-linux`). **Nothing anywhere makes a release depend on the test suite passing**, which is the mechanism by which v1.3.0 and v1.4.0 were both published green while `CI` was red on the same SHA. Verified on `5f127eb`: run `33284439253` (Release) succeeded and run `33284438194` (CI) failed, same commit, same minute. **Mechanism, specified (panel rounds 1 and 2, 2026-08-29): a reusable workflow, not a cross-workflow `needs:`.** GitHub Actions has no `needs:` across workflows. Extract test/fmt/clippy into `.github/workflows/checks.yml` with `on: workflow_call`; `CI` calls it on push, `Release` calls it as a `checks` job. One definition, invoked from both places, so the gate cannot drift from what CI runs.

  **Name it honestly: this gates on the shared check definition, not on the prior CI workflow run.** Release re-runs the checks for the tagged SHA rather than querying CI's result, deliberately: a tag push does not trigger CI (CI is `push: branches`, Release is `push: tags`), so there is frequently no CI run for that SHA at all, and `workflow_run` would either race or fail open.

  **Dependency placement (panel round 2 correction):** `needs: checks` goes on **`build-linux` and `build-macos`**, not only on `create-release` and `docker`. Release builds, archives, checksums, and uploads artifacts inside the build jobs (`release-and-publish.yml:67,83,123,137`), so gating only the publish jobs would still produce and upload artifacts off a red suite.

  **Two Actions wiring details that will bite otherwise (panel round 2):** caller workflow-level `env` does **not** propagate into a called reusable workflow, so `checks.yml` must define `RUST_VERSION` itself or accept it as an input; and permissions passed to a called workflow can only be *downgraded*, so the `checks` caller job sets read-only permissions explicitly rather than inheriting Release's `contents: write` / `packages: write` (`release-and-publish.yml:14`).

  **Testing this safely:** verify on a throwaway tag in a scratch fork or with `act`, never by pushing a deliberately-broken tag to `otto-rs/otto`, since a partial Release run publishes artifacts.

  **Scope note:** the reusable workflow extracts CI's test/fmt/clippy job, not its separate build matrix (`ci.yml:39`). Release has its own release-profile build matrix, so build coverage is not lost; the gate is named "shared checks" rather than "CI passed" to keep that distinction true.
- **Success criteria:** the `CI` workflow concludes `success` on a runner; a tag push whose shared checks fail produces **no uploaded artifacts, no GitHub release, and no GHCR image**; `git ls-files Cargo.lock` prints `Cargo.lock`; `ls makefiles/` contains no Tatari service name.

#### Phase 1: Silent-success criticals in the parse/schedule core
**Model:** opus

- [x] **OPEN** `parser.rs:499` (injection), `:823-856` (`resolve_default_tasks`) - bare `otto` executes the `Clean` builtin. `inject_builtin_commands` still runs before default-task expansion, which filters only the stale lowercase `"graph"` at `:839` while `BUILTIN_COMMANDS` are capitalized (`builtins.rs:14`). Reproduced: a project defining only build/test, bare `otto`, printed `Querying database for old runs... / No runs matching deletion criteria found`, exit 0. Fix: filter `BUILTIN_COMMANDS` in `resolve_default_tasks`/`parse_all_tasks`; delete the dead `"graph"` filters, now at `:602, :631, :839, :870`.
- [x] **OPEN** `parser.rs:2463-2478` - unknown task names silently dropped. `partitions()` starts from the first matching index and never emits `args[0..first]`. Reproduced: `otto nonexistent` exits 0 with "No tasks to execute"; `otto nonexistent build` silently runs build; `otto buld` exits 0. Fix: error when any arg is not consumed by a partition, with levenshtein near-match suggestions. **`levenshtein = "1.0"` (`Cargo.toml:23`) still has zero `src/` references**, so this bullet is also what justifies keeping the dep. **STAYS: NOT covered by `2026-08-29`** (named a Non-Goal there).
- [x] **CHANGED** `scheduler.rs:899-906` - dependency cycles. **Corrected description:** the cycle is no longer silent (v1.3.0 `121f384` made skips print, so `a -> b -> a` now emits `[a] skipped (unreachable dependency)` / `[b] skipped (dep a skipped; cascade)`), but **exit is still 0 and there is still no cycle detection on the execution path**. Post-loop reconciliation marks blocked tasks Skipped and `execute_all` returns `Ok(())` at `:906`. The daggy detector works: `otto Graph` on the same file exits 1 with `Failed to add edge to b: WouldCycle`. Fix: topological validation at scheduler init reusing `DagVisualizer::create_dag_from_tasks`; independently, `execute_all` returns Err when zero tasks reached a terminal state.
- [x] **OPEN** `scheduler.rs:822` - task name parsed out of the error string (`error_str.split_whitespace().nth(1)`); channel still `mpsc::Sender<Result<String>>` (`:569`, `:910`). Reproduced: a `python:` task with `PATH` lacking `python3` printed exactly `[such] failed`, downstream never ran, and the run hung until killed at 12s (exit 124). Fix: typed channel payload carrying name + error + exit code structurally.
- [x] **OPEN** `scheduler.rs:933,967-974,986,1005,1031-1032` - early `?` exits inside the spawned task body bypass `tx.send` entirely (semaphore acquire, dep double-check, `create_dir_all`, `symlink`, `ActionProcessor::new/process`). Handles are stored at `:491,:562` and removed at `:783,:855` but never awaited; no `JoinSet`. Fix: wrap the body so every exit path sends exactly one message; reap handles via JoinSet so panics are logged.
- [x] **OPEN** `parser.rs:742-744,747-766` - `otto: tasks: []` plus an explicit task request prints nothing and exits 0. Reproduced, both halves: `otto build` with `tasks: []` produced no output at exit 0, and so did `otto help build extra`. Fix: only consult default_tasks when args are empty; make `show_help` exhaustive.
- [x] **CHANGED** `parser.rs:678-684` - `-j 0` accepted, run never terminates. **Mechanism corrected:** it is a hot spin at **99.3% CPU** measured on the live pid, not a semaphore block. `while active_tasks.len() < max_concurrent` (`scheduler.rs:621`) with 0 never admits a task, and the sweep at `:665-692` `continue`s forever. `permits_for`'s `debug_assert!(max_parallel >= 1)` (`:51-54`) is never reached, so `2026-08-28`'s claim that its Phase 7 "asserts it" is false for `-j 0`. Fix: `value_parser!(u64).range(1..)`.
  - **DROPPED** sub-bullet: "also validate `jobs: 0` at config load". `otto.jobs` has zero consumers, so `jobs: 0` in an ottofile runs fine and validating it would guard nothing. Folded into the inert-`otto:`-keys item in API Design instead.
- [x] **CHANGED** `scheduler.rs:441-448,504,1288-1290` - conditionally-skipped tasks invisible in terminal mode. **Half shipped** by `121f384`: `mark_skipped` now prints, and the up-to-date skip prints at `:504`. **Still open:** the run record. `get_skip_reasons()` at `:1288-1290` has zero callers in `src/`, so skip provenance is still built and dropped rather than persisted.
- [x] **OPEN** `scheduler.rs:1202-1205,1259-1264` - the DB-recorded exit code is always 1. Reproduced: `bash: exit 7` formats `{:?}` as `Some(7)`, the recovery parse fails, `unwrap_or(1)` wins, and `sqlite3 otto.db` shows `boom|1|failed`. Fix: carry `status.code()` structurally on the typed channel payload.
- [x] **CHANGED** `parser.rs:1028` + 12 `process::exit` calls - **got worse, not better.** The top-level calls did become `try_get_matches_from` (`:396`, `:586`, commit `1390be4`), but one `get_matches_from` remains at `:1028`, so `otto build --bogus` still exits 2 from library code. `process::exit` went from 8 sites to 12 (`:403,430,445,453,458,508,510,528,532,538,549,774`); **four are new v1.3.0 work** (`--tasks` added `:528,532,538` in `e647d48`; `foreach: command:` added `:508` in `fb864a8`). Fix unchanged: propagate, exit only in main.
- **Success criteria:** `otto nonexistent` exits non-zero naming the unknown task; a 2-cycle exits non-zero; a spawn failure names the real task and the run terminates; `-j 0` is rejected at parse; `exit 7` is recorded as 7; `git grep -c 'process::exit' src/cli/parser.rs` returns 0.

#### Phase 2: Conditional-deps and foreach semantics
**Model:** opus

- [x] **CHANGED** `scheduler.rs:517-518,729-730` - foreach with up-to-date subtasks aggregates the virtual parent to Skipped, killing everything downstream at exit 0. Reproduced on a warm cache: `[build:a] skipped (up to date)` / `[build:b] skipped (up to date)` / `[build] skipped` / `[deploy] skipped (dep build skipped; cascade)`, rc=0, while the identical non-foreach shape ran `deploy`. **Cause corrected:** up-to-date skips do land in `completed_set`, but carry `TaskStatus::Skipped`, and the aggregation override reads *status* only, so it cannot distinguish an up-to-date skip from a gated-out one. **The fix site is the aggregation override at `:729-730`, not `classify_edge`.** No commit has touched it; no test pins it.
- [x] **CHANGED** Serial foreach with a mid-chain failure. **Premise replaced:** the chain `before:` edges this bullet assumed are gone (`121f384` made serial an ordering property via `SerialGroups`, `scheduler.rs:208-278`). **Current semantics, reproduced:** `[step:b] failed` -> `[step:c] skipped (serial predecessor step:b failed)` -> `[step] skipped (dep step:c skipped; cascade)` -> `[fixer] skipped (dep step skipped; cascade)`, rc=1. The same ottofile with `parallel: true` gives `[step] failed` and the fixer runs. So `2026-08-28`'s stated "parent aggregates Failed when any member failed" is **true for parallel and false for serial**: the parent's `When::Always` edges to its subtasks (`parser.rs:1508-1519`) hit the Skipped short-circuit at `scheduler.rs:176-178` and the parent is skipped before the aggregation arm can run.
  **This bullet must change a shipped test, not add one.** `tests/serial_foreach_test.rs:216-266` deliberately asserts `statuses["up"] == Skipped` and documents in its own comment that aggregation "never gets a chance to fire". Inverting `:176-178` per the decision below inverts that test.
- [x] **OPEN** `when: always` cascade semantics, same `classify_edge` site (`scheduler.rs:176-178`). Cleanup attached to a cascade-skipped task is itself skipped, exactly when a chain broke and cleanup matters. Reproduced: `[a] failed` -> `[b] skipped (dep a failed...)` -> `[cleanup] skipped (dep b skipped; cascade)`. **The code contradicts itself in two places**, not just against the docs: `parser.rs:1505-1507` comments that Always edges queue the parent "after all subtasks reach a terminal state (Completed | Failed | Skipped)", and `scheduler.rs:955-963` already resolves `(When::Always, Skipped)` to satisfied, while `:176-178` returns Unreachable before either can apply. **Decision stands: Always + skipped source = Satisfied**, per the provenance table in Architecture.

  **Ordering within Phase 2, mandatory (panel rounds 1 and 2, 2026-08-29):** this edit alone does **not** fix the warm-cache foreach bullet, and done alone it introduces a runtime failure. There are **four** sites, not one:

  1. **Introduce `SkipKind` provenance.** `skipped_set` is a bare `HashSet<String>` today (`scheduler.rs:613`) and carries no kind, so `classify_edge` has nothing to branch on. It becomes `HashMap<String, SkipKind>`. **Which of the three sets survive:** `completed_set` (`:469`) and `failed_set` (`:475`) stay as they are; only `skipped_set` becomes keyed, because it is the only one whose members need a reason distinguished. Every reader of it changes signature: `mark_skipped` (`:441`, accepts and inserts the kind), `skip_reason_for` (`:299`, today rebuilds free text from `skipped.contains(..)` and should derive its string from the kind instead of guessing), `classify_gates` (`:476`), `SerialGroups::classify` (`:533`, which today folds skipped predecessors into unreachable without distinguishing them), the up-to-date skip write (`:516`, which is the site that must start emitting `SkipKind::UpToDate`), and the post-task routing (`:761-775`) where a virtual parent that aggregated to Skipped inserts its own kind.
  2. **Fix the aggregation override** at `scheduler.rs:727-731`, which independently collapses "any subtask Skipped" to parent `Skipped` (`any(Failed) -> Failed`, `else any(Skipped) -> Skipped`, `else Completed`). `UpToDate` must count as success-like here or the warm-cache bullet stays broken no matter what `classify_edge` does.
  3. **Update the worker's dependency double-check** at `scheduler.rs:955-963` to the same table. It is a second gate on the same edge and it must not disagree with the first. With the corrected table its `(When::Failure, Skipped) -> _ => false` fallthrough is now *right* and stays; what changes is `(When::Success, Some(Skipped)) => true` at `:957`, which today accepts every skip kind and must narrow to `UpToDate` only. Had the earlier draft's `failure` cells shipped, this site would have rejected at `:969` exactly what `classify_edge` had just admitted, aborting the task at spawn time.
  4. **Reconcile `SerialGroups::classify`** at `:533` with the table, since it independently decides that a skipped predecessor blocks its successors and is the source of `SkipKind::SerialPredecessor`.
  5. **Then invert `classify_edge`** at `:176-178`.

  Steps 1 through 4 land in the same commit as, or before, step 5. An earlier draft claimed one edit closed two bullets; that was wrong, a later draft named three of the five sites, and the draft after that named four. The count moved twice under review, which is the argument for the nine-cell matrix test: the contract has to be asserted in one place rather than re-derived at each gate.
- [x] **OPEN** Paired `when: success` + `when: failure` edges on the same source are accepted and make the dependent permanently skipped at exit 0. Reproduced: `[dep] skipped (dep src succeeded; this task required when: failure)`, rc=0. The conditional-deps design doc claims a config-load validation pass rejects them; `grep` finds no such check. `compute_task_deps_from_specs` (`parser.rs:1389-1433`) still validates only that dep names exist, and is still the right site.
- [x] **CHANGED** `scheduler.rs:1064-1110` + `app.rs:453-457` - child-process lifecycle. `kill_on_drop` and `process_group` appear nowhere in `src/`. **The TUI half is partly stale:** `app.rs` now awaits `scheduler_handle` after the TUI exits, so quitting no longer abandons children; it strands the user waiting with no cancel path and no message. Fix: `kill_on_drop(true)` at minimum; spawn children in their own process group; give `execute_all` a cancel signal the drain loop honors, wired to the TUI quit path.
- [x] **OPEN** `scheduler.rs:635-643` - fragile ready-queue spin guard (`ready_queue.len() == 1`). Statically unreachable today (entries enter only via the all-Satisfied sweep at `:674` or the up-to-date branch at `:521-542`), one refactor away from a busy-spin. Fix: never enqueue non-Satisfied tasks; route them to `blocked_tasks`.
- [x] **OPEN** `scheduler.rs:109-137` - `env_to_json` corrupts multiline values. Reproduced: `MULTI='line1\nline2\nline3'` in the `.env` file becomes `{"MULTI": "'line1"}` in the `.json`, the consumer printed `GOT=['line1]`, and the run exited 0. Fix: escape newlines on write or parse quoted values.
- [x] **OPEN** Minors batch, **all line numbers re-anchored**: duplicate `#[serial]` (`scheduler.rs:1519-1520`); stale `#[allow(dead_code)]` on `to_tui_status` (`:424`) while it is used at `:753`; dead `in_degree` bookkeeping (`:585-593`, `:811`); magic channel sizes to named consts (`scheduler.rs:569` = 32, `output.rs:168` = 10000, `app.rs:397` = 1000); parent-derivation logic tripled at `executor/task.rs:123`, `:234`, `executor/graph.rs:769`; silent edge-drop `debug_assert` at `executor/graph.rs:168-171`. **Path correction:** the draft cited `src/cli/commands/graph.rs`, which does not exist; the file is `src/executor/graph.rs`.
- **Success criteria:** `classify_edge_skip_provenance_matrix` asserts all nine `SkipKind` x `when` cells and passes; the same nine cells are asserted against the worker's dependency double-check, not only `classify_edge`, so the two gates cannot drift; a warm-cache foreach runs its downstream task; a serial foreach with a mid-chain failure runs its `on-failure:` fixer and `tests/serial_foreach_test.rs:216-266` is updated to assert Failed rather than Skipped; `when: always` cleanup runs on a cascade-skipped source; paired success+failure edges are rejected at config load.
- **Explicitly unchanged:** `tests/foreach_aggregation_test.rs` stays as-is, including `:204` and `:260` which pin "Skipped is not Failure". Exactly one shipped test is deliberately inverted by this phase (`serial_foreach_test.rs:216-266`); any other test that goes red is a regression, not an expected update.

#### Phase 3: Containment - injection, deletion safety, output integrity
**Model:** opus

- [x] **OPEN** `action.rs:205` (bash env), `:254` (bash param), `:521` (python env), `:568` (python param) - shell/python injection via values templated raw into generated scripts. Reproduced, both vectors: env `PAYLOAD: 'x"; touch /tmp/OTTO_PWNED_ENV; echo "y'` printed `[hello] y` then `[hello] payload=x` and created the file; the same payload through `--name` created its file too. `git log -S 'export {key}='` shows the last touch predates this doc. Fix: pass values through the process environment (the scheduler already calls `.envs(&envs)` at `scheduler.rs:1067`); where materialized, single-quote-escape exactly as `otto_serialize_output` already does. Regression tests with `"`/`'`/`$()`/`;` payloads through both generators.
- [x] **NEW** Dynamic-source values reach the shell raw, and one escapes the workspace. `foreach: command:` and `choices-command` (v1.3.0) feed the same generators. Reproduced: foreach item `a"b` emits `export OTTO_FOREACH_ITEM="a"b"` / `export item="a"b"` and a bash syntax error; foreach item `../../../ESCAPED` created `~/.otto/<proj>/<ts>/ESCAPED`, **outside the run's `tasks/` tree**; foreach item containing `${IFS}` aborts the task env with `Environment variable 'IFS' not found`, i.e. command output is re-expanded as syntax; `choices-command` value `a";touch${IFS}/tmp/OTTO_PWNED_CHOICES;echo"b` created the file. The only sanitizing is space-to-underscore on the *identifier* (`task.rs:209`); the *value* is untouched. Fix: rides the escaping fix above, plus reject or slugify foreach items containing path separators before they become directory names.
- [x] **OPEN** `cfg/env.rs:117-125` - command-substitution output re-expanded as variable references (data treated as syntax). Reproduced: `LEAK: "$(echo '$PARENTVAL')"` with `PARENTVAL=leaked-parent-value` in the parent env printed `LEAK=[leaked-parent-value]`. Output containing an undefined `$word` fails the whole config load. Fix: substitute command output via a placeholder map applied after variable resolution.
- [x] **OPEN** `cfg/env.rs:327-330` - bare `$VAR` silently unresolved when `${VAR}` appears in the same value. The generated script is the proof (a terminal echo masks it, since bash re-expands the raw template): `BOTH: "a=${FOO} b=$FOO"` generates `export BOTH="a=fooval b=$FOO"`. Delete the guard.
- [x] **OPEN** `clean.rs:271,311,234` + `state/manager.rs:884` - clean follows symlinks under `~/.otto` and `remove_dir_all`s real targets outside it. Reproduced under a scratch `HOME`: with `fakehome/.otto/otto-deadbeef -> $TMPDIR/otto-victim`, `otto Clean --no-db --keep-days 1` reported `Deleted [deadbeef] ... (9 B)`, the victim directory was gone and the symlink was intact. `path.is_dir()` follows the link; there is no canonicalize-under-root anywhere. Fix: skip symlinked entries via `file_type().is_symlink()` in both scans, and canonicalize-and-assert-under-root before every `remove_dir_all`.
- [x] **OPEN** `executor/task.rs:45-46` + the four generator functions in `action.rs` - nondeterministic script hash from HashMap iteration order. Reproduced: six runs of one unchanged ottofile with 6 `envs` keys produced six cache files (`18ff3d55 254de8b2 9a1a0db9 9f0f2ecc a0fa34e6 c6e67f5c`), so the content-addressed cache never hits and files accumulate. Sort keys in all four generators; add a hash-stability test.
- [x] **OPEN** `ports/fs.rs:156-158` + `action.rs:105-114` - non-atomic `fs::write` plus write-only-if-absent caching permanently poisons a torn script. Reproduced: truncating a cached script to 60 bytes made the next run re-symlink and execute the stump, which emitted bash's `declare -a DIRSTACK=()` dump and **reported `finished successfully`**, leaving the cache byte-identical for every future run. Fix: atomic write in the FileSystem port (temp in target dir + rename); optionally validate cache hits.
- [x] **OPEN** `executor/output.rs:202` - `while let Ok(n) = reader.read_line(...)` treats the InvalidData error from the first non-UTF-8 byte as EOF. Reproduced: a 5-line action with `printf '\xff\xfe bad\n'` as line 2 left both the terminal and `stdout.log` containing only `line1`, exit 0. Fix: `read_until(b'\n')` on bytes + `from_utf8_lossy` for display, mirroring `TeeWriter::write`; never let a decode error end the drain.
- [x] **CHANGED** `cfg/env.rs` remainder:
  - ~~nested `$()` truncates at first `)`~~ **SUPERSEDED, verified true**: `10eb334` replaced the regex find with a quote-aware level stack at `:175-200`, added an up-front unmatched-`$(` check at `:29-35`, and pinned it in `tests/env_command_substitution_test.rs:47,63,93`.
  - **OPEN** no escape for a literal `$`: `$$` is unhandled, the regex at `:322` skips it, and bash then expands it to a PID. Support `$$` or document.
  - **CHANGED** ">100-deep linear chains falsely report circular" is wrong as written: 105-deep succeeds; **200-deep prints `Warning: Failed to evaluate global environment variables: Maximum iterations reached ... possible circular dependency` and then continues with the globals silently dropped.** It is a warning, not an error, the threshold is ~200, and it is HashMap-order dependent. Fail closed instead.
  - **CHANGED** per-call `Regex::new(...).unwrap()` at `:307`, `:322` is not cosmetic: measured 12.1s for a 100-deep chain, 22.3s at 150, 45.0s at 200. Move to `LazyLock` statics.
  - **OPEN** a real cycle reports the wrong message (`:57-72` returns "Failed to resolve environment variable 'X'").
- **Success criteria:** a `"`-bearing env value, param, foreach item, and choices value all reach the task as literal text and create no file; a foreach item containing `/` cannot create a directory outside `tasks/`; six consecutive runs of one ottofile produce one cache filename; a truncated cache entry is detected rather than executed; a non-UTF-8 byte mid-stream does not truncate the log.

#### Phase 4: State and DB integrity
**Model:** opus

Nothing in v1.3.0 or v1.4.0 touched these paths; the only commit that landed on them was clippy-only (`037a4dc`). **All nine bullets OPEN, one with a corrected scope.**

- [x] **OPEN** `state/migrations.rs:59-62` + `schema.rs:156` - non-idempotent v1-to-v2 migration with a crash window that permanently bricks the DB. `migrate_v1_to_v2(conn)?` then `set_version(conn, 2)?` are two separate statements with no `conn.transaction()`; `schema.rs:156` still fires a bare `ALTER TABLE projects ADD COLUMN name TEXT` with no `pragma_table_info` guard. `manager.rs:104-106` `try_new()` is still `Self::new().ok()` with zero logging. No v1-to-v2 test exists (`migrations.rs` tests cover fresh/idempotent/set_version only). Fix: each migration step + its set_version in one transaction; pragma_table_info idempotency check; `warn!` in try_new and callers; a real v1-to-v2 test plus the crash-window test.
- [x] **OPEN** `schema.rs:97` + `manager.rs:164,843` - global `UNIQUE(runs.timestamp)` in seconds, so two concurrent runs in the same second collide and the loser's run plus all task records vanish. Completion still updates `WHERE timestamp = ?5` and deletion still selects `WHERE r.timestamp = ?1`. Fix: drop the constraint; key completion/deletion off `runs.id`.
- [x] **OPEN** `state/db.rs:27-38` - no `busy_timeout`. Open sets only `journal_mode=WAL` (`:30`) and `foreign_keys=ON` (`:34`); repo-wide grep for `busy_timeout` and `synchronous` returns zero hits. `scheduler.rs:1267` still discards the record error with `let _ =`. Fix: `busy_timeout` named const + `synchronous=NORMAL` at open; stop discarding record errors.
- [x] **OPEN** `workspace.rs:354` - `record_run_complete_in_db` has zero production callers. Its **only two references in the whole tree** are the definition and its own unit test at `:1082`; nothing in `app.rs`, `scheduler.rs`, or the TUI calls it, so every run stays `status='running'` and skews stats/history/clean. Wire it into run teardown in app.rs (both paths); end-to-end test asserting a completed run is marked complete.
- [x] **OPEN** `manager.rs:880` - `delete_run(.., true)` reconstructs `otto-<hash>` while `workspace.rs:135` builds `<name>-<hash>`. Live `~/.otto`: **222 project directories, 2 of which start with `otto-`.** DB-driven clean reports success, deletes the rows, and leaves the directories orphaned and invisible. `manager.rs:867` also hardcodes `dirs::home_dir()`, ignoring `OTTO_HOME`, and `:883` silently skips a missing directory. Fix: record the run directory at run start; honor `OTTO_HOME`; report rather than skip a missing directory. **The same prefix bug is at `clean.rs:282` and `pruning.rs:90`**: three sites, one `run_root()` helper.
- [x] **OPEN** `clean.rs:184,190` - `--keep-last` INVERTED in filesystem mode: an ascending (oldest-first) sort followed by `runs_to_delete.split_off(len - keep_last)` returns the tail, so **the newest `keep_last` runs become the delete list and every older run survives.** Every `keep_last` test in clean.rs (`:964`, `:1022`) is DB-mode, which is why it shipped. Fix: keep the N newest; fs-mode regression test asserting which timestamps survive.
- [x] **OPEN** Backend parity batch, all three confirmed. `keep_failed` is plumbed to the DB path at `clean.rs:96` and never referenced inside `execute_with_filesystem` (`:174-261`). `clean.rs:288` uses `dir_name.contains(filter)` vs the DB's `p.hash = ?1` (`manager.rs:794`). `MemoryStateStore` uses `run_count.saturating_sub(1)` (`ports/db.rs:480`) vs the real impl's raw `run_count - 1` (`manager.rs:856`), and the retention loop is duplicated line-for-line (`ports/db.rs:418-455` = `manager.rs:769-834`). Extract one shared pure retention function; pin RealFs/MemFs equivalence.
- [x] **OPEN** `state/db.rs:72-81` - `with_connection` locks a `std::sync::Mutex` and runs blocking rusqlite I/O inline on the tokio runtime. **Repo-wide grep for `spawn_blocking`: zero hits.** Route recording through `spawn_blocking` or a dedicated writer task.
- [x] **CHANGED** manager.rs error-conflation batch, line numbers re-anchored: `serde_json::from_str(..).ok()` nulls corrupt args (`:322`); unknown status strings become Failed (`:325`, `:346`); `unwrap_or(0)`/`unwrap_or(0.0)` mask stat-query errors (`:385-393`); `record_run_complete` ignores rows-affected (`:161-172`); `ensure_project` SELECT-then-INSERT race (`:894-934`, use `ON CONFLICT ... DO UPDATE`); `delete_run` not transactional with `run_count - 1` able to go negative (`:837-862`, `:856`); production `.unwrap()` at `:423` and `:781`; `started_at` read as non-optional at `:221-224` while the column is nullable (`schema.rs:126`) and `record_task_skipped` (`:240-250`) inserts rows without it. **Scope correction:** the `get_all_task_stats` vs `get_task_stats` `p.name` nullability split is real (`:447` reads `Option<String>` and falls back to hash at `:452`; `:598` reads `String` and errors on NULL) but only reachable after a partially-applied v2 migration, so it is a consequence of the migration bug rather than an independent one.
- [x] **OPEN** `pruning.rs:12-19` vs `state/db.rs:54-61` - `resolve_otto_home()` reads `OTTO_HOME` then `$HOME/.otto`, while `default_db_path()` reads only `OTTO_DB_PATH` then hardcodes `$HOME/.otto/otto.db`, so run dirs and DB can live in different trees. Derive the DB path from `resolve_otto_home()`. Also: no mtime grace period anywhere in `prune_orphaned_cache` (`:73-140`) for the concurrent-run race.

  **Reproduced live 2026-08-29 while verifying Phase 1, and it is worse than "can live in different trees": setting `OTTO_HOME` alone silently writes to the developer's real database.** Three probe runs under `OTTO_HOME=/tmp/p1check/.otto` put run directories in the scratch tree, left the scratch `otto.db` at **0 bytes**, and added a `p1check` project plus three `boom` task rows to the real `~/.otto/otto.db` (task count 4283 -> 4286). Re-running with `OTTO_DB_PATH` also set left the real DB unchanged and wrote `boom|7|failed` to the scratch DB. So `OTTO_DB_PATH` is currently the only isolating knob, and every "scratch `OTTO_HOME`" claim made anywhere in this document or its verification history is false for the DB half. The rows were deleted and the DB backed up first.

  **Target precedence after the fix:** `OTTO_DB_PATH` > `resolve_otto_home()/otto.db` > `$HOME/.otto/otto.db`. `OTTO_HOME` becomes the single knob that moves run dirs and the DB together, which is what every caller already assumes. `OTTO_DB_PATH` is kept, but demoted from an independent hardcoded path to an override of a derived default: it is a legitimate escape hatch (shared store, tmpfs, two projects pointed at one DB) and removing it would break existing use.

  - [x] **NEW (2026-08-29)** Pin it with a test asserting that setting **only** `OTTO_HOME` puts `otto.db` underneath it. Nothing currently pins this, which is exactly why it went unnoticed; the existing tests all set `OTTO_DB_PATH` explicitly and therefore cannot observe the bug.
  - [x] **NEW (2026-08-29)** Collapse the now-redundant `set_var("OTTO_DB_PATH", ..)` test scaffolding at `action.rs:744` and `scheduler.rs:1632`. Once the default derives from `OTTO_HOME`, these two call sites keep masking the defect in exactly the tests that should catch it. Either drop them in favor of setting `OTTO_HOME`, or keep one deliberately as the explicit-override regression test and say which.
- **Success criteria:** a completed run has `status='completed'` in the DB; `exit 7` is recorded as 7; two runs started in the same second both persist; fs-mode `--keep-last 2` leaves the two newest; a DB-driven clean removes the actual `<name>-<hash>` directory; an interrupted v1-to-v2 migration is resumable; **a run under a scratch `OTTO_HOME` with `OTTO_DB_PATH` unset writes its rows under that `OTTO_HOME` and leaves `$HOME/.otto/otto.db` byte-identical.**

#### Phase 5: Upgrade and HTTP safety
**Model:** opus

- [x] **NEW, blocks the rest of this phase** `upgrade.rs:335,352,432` - **`otto Upgrade` cannot complete an install.** `download_with_progress` binds `let temp_dir = tempfile::tempdir()?`, writes `otto.tar.gz` inside it, and returns `Ok(file_path)`; `TempDir` drops at function exit and recursively removes the directory, so `install_from_archive` opens a path that no longer exists. Verified against this repo's own `tempfile` rlib: `inside f: exists=true`, `after return: exists=false`. Fix: return the `TempDir` alongside the path (or `.keep()` it and take ownership of cleanup) so the archive outlives the call.
- [x] **OPEN** `upgrade.rs:463-464,227` - the install step is `fs::copy` over the running exe: ETXTBSY on Linux, non-atomic even where it works, and the comment at `:463` still reads `// Atomic replace (rename is atomic on most filesystems)`. The rollback path is identical at `:227`. Staging happens in a `tempfile::tempdir()` at `:436`, not beside `current_exe`, so a naive `rename` would cross filesystems. Fix: stage the new binary in the same directory as `current_exe`, set perms, `fs::rename`. Same for rollback. **Note: unreachable until the bullet above is fixed.**
- [x] **CHANGED** `upgrade.rs:322-353,430-467` - no integrity verification; the only check is `verify_binary` (`:469-482`) running the download with `--version`. Grep for `sha256`/`checksum` in upgrade.rs returns zero hits. **Premise corrected:** `install.sh:53-60` verifies `${asset_url}.sha256`, a **per-tarball sibling URL**, not a combined release checksums asset. The Upgrade builtin should verify that same sibling.
- [x] **CHANGED** HTTP safety, **retargeted**: `ports/http.rs` is dead code. `HttpReleaseFetcher`/`ReleaseFetcher` have zero call sites outside the `pub use` at `ports/mod.rs:7`, so the timeout, `error_for_status`, and `tempdir()?.keep()` leak (`http.rs:96`) findings all sit in a module nothing runs. **The live path is upgrade.rs's own inline reqwest:** `Client::new()` at `:286` and `:323` with no `.timeout(..)` and no client reuse, and the download at `:324` has no `error_for_status()` (`fetch_releases:297` does check). Fix: apply timeout + status check + single reused client in upgrade.rs; send the dead port to Phase 9's dead-code sweep, which also disposes of its 8 `lock().unwrap()` sites.
- **Success criteria:** `otto Upgrade` completes an install end-to-end against a fixture release; a checksum mismatch aborts before replacing the binary; a stalled connection times out rather than hanging; `git grep -c ReleaseFetcher src/` returns 0 after the dead port is removed.
- **Rollback criterion (panel round 2):** this phase changes rollback behavior, so rollback is asserted, not assumed. After a successful upgrade, `otto Upgrade --rollback` restores the previous binary and `otto --version` reports the prior version; and an upgrade interrupted between staging and rename leaves the original binary intact and executable. Both are tested against a fixture release, never against the developer's installed binary.

#### Phase 6: cfg correctness
**Model:** opus

- [x] **OPEN** `cfg/param.rs:343` - `nargs: "0:5"` panics: `attempt to subtract with overflow` at `Self::Range(min - 1, max)`. Reproduced. Sibling validations also absent: `"1:2:3"` yields `Range(0,2)` with the third part silently dropped, `"5:2"` yields an inverted `Range(4,2)` and is accepted, `""` errors only incidentally. **Sharper than the draft recorded:** `.nargs` has **zero readers outside `cfg/param.rs`**, and `num_args`/`value_delimiter` appear nowhere in `src/`, so `nargs` is parsed, serialized, and never used. `dest` (`:81`) and `constant` (`:90`) are likewise read nowhere outside their own tests. Per the API Design decision: wire `nargs` to clap `num_args` (space-separated, never `value_delimiter`), validate ranges, reject the malformed forms; delete `dest` and `constant`.
- [x] **SUPERSEDED, verified true** (`2026-08-29-strict-ottofile-schema.md` Phase 4, commit `b5a86dc`) `deny_unknown_fields`. Landed on six structs, one more than this bullet asked: `config.rs:16`, `otto.rs:133`, `otto.rs:189`, `task.rs:54`, `task.rs:369`, `param.rs:53`. Both motivating cases are dead: `task:` gives ``unknown field `task`, expected `otto` or `tasks` at line 4 column 1``; `befor:` gives ``tasks.b: unknown field `befor`, expected one of `help`, `after`, `before`, ... at line 6 column 5``. Seven pinning tests plus the inventory drift test `ottofile_reference_key_inventory_is_exhaustive` (`task.rs:1427`, tightened in `5f127eb`). The sub-clause "fix the param tests that exploit the leak" was a no-op: `b5a86dc` is insertion-only, +134/-0.
- [x] **OPEN** `cfg/task.rs:518,523` - shebang-with-args corruption. Reproduced through `ConfigSpec`: `#!/bin/bash -euo pipefail` + `echo hello` re-emits as `bash: |2-` / `   -euo pipefail` / `  echo hello`, reparses successfully, and `config == reparsed` is **false**. Cause is a prefix rather than full-line match (`.strip_prefix("#!/bin/bash")`). **`tests/roundtrip.rs` contains no `#!` anywhere**, so the invariant this bullet says is "locked" was never tested for shebangs at all. Strip the shebang only when the full first line matches; add shebang-args round-trip cases.
- [x] **OPEN** `cfg/task.rs:559` - multibyte-whitespace dedent panic in `deserialize_script_string`. Reproduced with U+2002: `start byte index 2 is not a char boundary; it is inside '\u{2002}'`. The byte slice is `line[min_indent..]` at `:559`. Dedent on char boundaries.
- [x] **OPEN** `cfg/task.rs:15` + `cfg/param.rs:11` - HashMap-backed `TaskSpecs`/`ParamSpecs` make serialization nondeterministic. Reproduced: five serializes of one 5-task config gave five distinct orders. Move to IndexMap (author order); add a determinism test. **Dependency note:** `indexmap` is currently **transitive only** (`cargo tree -i indexmap` shows it arriving via `petgraph` -> `daggy` and via `serde_yaml`), so this is a new *direct* dependency, added via `cargo add`.
- [x] **OPEN** `cfg/param.rs:354` - `divine` accepts garbage silently and is still infallible. All four cases reproduced: `-v|-x` yields one param `v` with `-x` dropped; `--foo|--bar` yields long `"foo--bar"`; `-verbose` becomes a positional named `"-verbose"`; and `-v|--verbose` plus `--verbose` in one map collapses to a single param with `short=None`, silently losing the short. Make divine return Result rejecting malformed keys; error on duplicate divined names in the map visitor. **STAYS: NOT covered by `2026-08-29`**, which names it a Non-Goal at its lines 58 and 358. Annotation verified true.
- [x] **OPEN** `cfg/task.rs:425-441` - `bash:` + `python:` on one task: bash silently wins. Reproduced: printed `FROM_BASH`, `finished successfully`, no warning. The chain is `if let Some(bash) ... else if let Some(python) ... else if let Some(action)`. Error when more than one is present.
- [ ] **MOVED OUT** ~~snake_case to kebab-case key flip~~ - see Addendum. **The premise inverted.** Observed 2026-08-29: `otto.retention.keep_days: 5` **loads and runs fine**; `keep-days: 5` **hard-errors** with ``otto.retention: unknown field `keep-days`, expected one of `keep_days`, `keep_last`, `keep_failed`, `auto_prune`, `prune_interval_hours```. So `deny_unknown_fields` did not make snake_case an error, it made the *kebab* spelling a hard error where it was previously ignored and defaulted. (`max_items` is not in this schema at all; the retention keys are those five.) The schema is still all-snake with no `rename_all` (`cfg/otto.rs:135-155`) while `on-failure` (`task.rs:410`) and `choices-command` (`param.rs:100`) are kebab, so the inconsistency is real. But the flip now has to invert a shipped test (`deny_unknown_fields_names_a_kebab_retention_key_the_schema_keeps_snake`, `otto.rs:364`), update the published 44-key inventory in `docs/commands/ottofile-reference.md`, and satisfy the drift test at `task.rs:1427`. `2026-08-29:153` already ruled this needs "its own doc, with aliases and a deprecation window". Honoring that.
- [x] **OPEN** cfg minors batch, all confirmed: `Value` deserialize rejects bool/number while `default` accepts both (`constant: true` and `constant: 42` both give `invalid type`, `default: true` and `default: 42` both parse), because `deserialize_value` has no `visit_bool`/`i64`/`u64`; **zero `skip_serializing_if` in `src/cfg/`**, so one param emits `dest: null, metavar: null, default: null, constant: null, choices: [], choices-command: null, nargs: '1'` and the 17-line default `otto:` block is always emitted; `cfg/error.rs` is still present with a blanket allow at `:1` and its sole reference is `pub mod error;` at `cfg/mod.rs:4`; per-call `Regex::new(..).unwrap()` at `env.rs:307,:322` and map-get unwraps at `env.rs:44,:62` with no `LazyLock` anywhere in `src/cfg/`; `default_otto()` (`otto.rs:169`) vs `impl Default` (`otto.rs:219`) still duplicated; `namify` (`task.rs:687`) has one call site at `:720`. Entry-DEBUG logging: **all of `src/cfg/` has two `log::debug!` calls** (`otto.rs:62,:66`), and `evaluate_envs` (`env.rs:8`), `deserialize_param_map` (`param.rs:383`), and `expand_foreach` (`task.rs:610`) have none.

  **Success criterion, added 2026-08-30 because this bullet shipped without one and was skipped for exactly that reason:** a `ParamSpec` with only `help:` set round-trips byte-identically, emitting no `metavar: null`, no `choices: []`, no `nargs: '1'`; a config with no `otto:` block does not gain one; `git grep -c 'Regex::new' src/cfg/` returns 0; `git ls-files src/cfg/error.rs` returns nothing.

  **Closed 2026-08-30. Two sub-items were already moot and are recorded rather than invented into work:**
  - *`Value` bool/number support* - **premise gone.** Phase 6's own commit `18a1cd4` deleted `dest` and `constant`, and with them `deserialize_value` and `impl Deserialize for Value`. `git grep -n 'constant|\bdest\b' src/cfg/` and `git grep -c deserialize_value src/` both return nothing. There is no longer a deserializer to teach about bools.
  - *`LazyLock` for the `env.rs` regexes* - **already done by Phase 3**, which rewrote `resolve_env_variables` as a single pass and removed both. `git grep -c 'Regex::new' src/cfg/` returns 0.

  **What actually changed:** `skip_serializing_if` added across `ParamSpec` (`metavar`, `default`, `choices`, `choices-command`, `nargs`, `help`) and `OttoSpec` (`name`, `about`, `jobs`, `home`, `tasks`, `verbosity`, `envs`, `retention`), with `Nargs::is_one`, `RetentionSpec::is_default`, and five `is_default_*` predicates; `api` deliberately keeps no predicate, being the schema version. `src/cfg/error.rs` deleted (30 lines, blanket `allow` at `:1`, sole reference `pub mod error;`). `default_otto()` now delegates to `OttoSpec::default()` instead of duplicating nine fields. Function-level `log::debug!` added to `evaluate_envs`, `deserialize_param_map`, `expand_foreach`, and `expand_foreach_with_items`. Pinned by `a_minimal_config_round_trips_without_gaining_keys` in `tests/roundtrip.rs`.

  **Deliberately not done: inlining `namify` (`task.rs:801`, one call site at `:834`).** The bullet observes the single call site; it does not follow that inlining improves anything. `task_spec.name = namify(&name)` reads better than four lines of `split('|')`/`map_or_else` at the call site, and `namify` carries its own test (`:809`) that inlining would delete. Recorded as a judgment, not an oversight.
- [x] **NEW (2026-08-29, found while verifying Phase 2)** `cfg/task.rs:648-655` - **foreach subtasks share one `input`/`output` set, so per-item incremental builds are impossible.** `expand_foreach_with_items` builds each subtask with `let mut subtask = self.clone()` (`:648`) and then injects **only** environment variables: the loop variable, `OTTO_FOREACH_ITEM`, and `OTTO_FOREACH_INDEX` (`:653-655`). `input` and `output` are cloned verbatim and never rewritten per item, so a pattern like `input: ["src/${item}.txt"]` reaches the up-to-date check as the literal string `src/${item}.txt`, matching no file.

  **Two observable consequences, both reproduced 2026-08-29 against `c72f911`:**
  1. *Interpolated paths never go up to date.* A foreach over `[a, b]` with `input: ["src/${item}.txt"]` / `output: ["out/${item}.o"]` re-ran both subtasks on a warm second invocation even with `out/a.o` and `out/b.o` present and newer than their inputs.
  2. *Literal paths make the cache all-or-nothing.* With a single static `input`/`output` shared by every subtask - which is what Phase 2's own regression test `test_warm_cache_foreach_runs_its_downstream_task` (`tests/skip_provenance_test.rs:65`) had to use to warm the cache at all - all N subtasks skip together or none do. Touching one item's source re-runs every item.

  **This is a gap, not a regression:** it predates this document and no phase's premise depended on it. It surfaced only because Phase 2 made warm-cache foreach behave correctly, which made per-item caching worth having for the first time. Phase 2's fix is unaffected and stands.

  **Fix:** interpolate the foreach variable into each subtask's `input` and `output` the same way it is injected into `envs`, using the same `var_name` from `ForeachSpec` (so `as: pkg` makes `${pkg}` expand in paths too). Decide and record whether an unexpandable variable in a path is a config-load error or a warning; prefer the error, consistent with this document's fail-closed direction and with `deny_unknown_fields`. Regression test: a foreach whose per-item outputs are current skips exactly those items and re-runs exactly the one whose input was touched.

- **Success criteria:** `nargs: "0:5"`, `"5:2"`, and `"1:2:3"` are rejected with a message naming the param; a U+2002-indented script loads; a shebang-with-args survives a round-trip byte-identically; five serializes of one config produce one order; a params map with two shorts or a duplicate divined name is rejected. a foreach over `[a, b]` with per-item `input`/`output` paths re-runs only the item whose input changed.

#### Phase 7: Makefile converter truth
**Model:** opus

**Zero of seven landed.** `src/makefile/` has gained exactly one line (`tty: None`, `10f00e6`) since `a1b1217` on 2025-11-06, so nothing here has moved since before this doc was written.

- [x] **CHANGED** `makefile/parser.rs:217-262` - backslash continuations and space-indented lines silently corrupt the conversion. The draft's own example reproduced: a recipe truncated to `docker run --rm  --label foo` plus an invented task `-v /a:` with `before: ['/b','\']`; `SOURCES := a.c \` truncates and continued lines vanish. **Refinement:** tab-indented continuations **do** join correctly today; only the space-indented path corrupts, because `parse_commands`'s space-indent branch bails on `contains(':')` at `:238`. Fix: a preprocessing pass joining `\`-terminated lines for all line types as make does; treat space-indented recipe-looking lines as errors, not targets.
- [x] **OPEN** `makefile/converter.rs:50-53` - `$(shell ...)` preserved verbatim. Runtime, reproduced: `Failed to resolve environment variable 'VERSION': Command 'shell echo 1.2.3' failed with exit code 127`, and the sibling `NAME` is lost too because the whole global env map is abandoned. Strip the `shell ` prefix during conversion; warn on nested `$()` the evaluator cannot handle.
- [x] **OPEN** `makefile/converter.rs:109-137` - `$(VAR)` in recipes passes into bash verbatim, where it is command substitution expanding to empty, **and the task succeeds**. Reproduced: `NAME: command not found`, `VERSION: command not found`, `[build] Building  version`, `finished successfully`, exit 0. `build_bash_action` strips only `@` and `-`. Rewrite `$(VAR)`/`${VAR}` for known variable names to bash `${VAR}`; warn on automatic variables and unknown functions.
- [x] **OPEN** `makefile/parser.rs:119-194` - all five constructs confirmed: `export PATH := ...` parses to an env named `"export PATH"` (`:130`); `build: CFLAGS=-g` becomes `before: ['CFLAGS=-g']`; `test check: build` becomes one task named `"test check"` (`:185`); `install:: build` yields `before: [':','build']`; `%.o: %.c` becomes a task named `'%.o'` that **also becomes the default task and then fails to load** with `Task '%.o' has unknown dependency '%.c'`. Duplicate targets are silently last-wins at `converter.rs:69-73`. Fix: strip/record `export`; detect and warn on target-specific vars, multi-target, double-colon; skip pattern rules with a warning; warn on duplicate targets.
- [x] **OPEN** `cli/commands/convert.rs:15` - `--strict` is a dead flag; its only readers are its own unit tests at `:65`,`:75`, and `execute()` (`:23-52`) never branches on it. There is no line tracking (`MakefileParser` holds only `content`, `parser.rs:5-7`) and no warning accumulator anywhere in `src/makefile/`. Decision unchanged: **implement**. Every silent corruption above becomes a warning at minimum; `--strict` promotes warnings to errors.
- [x] **CHANGED** Converter minors, all confirmed: `jobs: num_cpus::get()` at `converter.rs:34` bakes the converting machine's CPU count into the output (emitted `jobs: 32` here); make-function-bearing variables are dropped with no else branch (`parser.rs:93`); `is_phony` is hardcoded false (`parser.rs:209` plus 8 converter sites) and `phony_targets` is read only in parser tests; dead `!trimmed.starts_with('\t')` at `parser.rs:100`. **Correction:** `?=` is parsed correctly as Conditional at `parser.rs:154`; the bug is that `converter.rs:54` then treats it as `=`. The precedence *documentation* is what is wrong, and it is wrong in the doc, not the code (see Phase 10).
- [x] **OPEN** Replace the fixture suite (coordinates with Phase 0's Tatari removal). All five fixtures are still present, three Tatari-named, referenced from `tests/makefile_converter_test.rs:6,26,52,68,84`, and the assertions are still non-empty-only (`:16-21,:36-40,:63,:79,:95`). `test_roundtrip_conversion` (`:99`) compares only `otto.tasks` and `tasks.len()` (`:130-131`); **nothing asserts the absence of `$(shell` or `$(VAR)`**. Add synthetic Makefiles covering the negative cases with expected-output comparisons, plus a semantic round-trip through ConfigSpec.
- **Success criteria:** a Makefile with a space-indented continuation is rejected or joined, never converted into an invented task; converted output contains no `$(shell` and no `$(VAR)`; a pattern rule produces a warning and no task; `--strict` turns each of those warnings into a non-zero exit. Per-bullet criteria added during implementation, because four of the seven bullets had no measurable clause of their own: **parser constructs** - `export PATH := x` converts to an env named `PATH`, `build: CFLAGS=-g` yields no task and no `before:` entry, `test check: build` yields two tasks named `test` and `check`, `install:: build` yields `before: [build]` with no `:` entry, and a duplicate target warns naming both line numbers; **converter minors** - converted output contains no `jobs:` key, a variable dropped for using a make function produces a warning naming it, and a `.PHONY` target parses with `is_phony: true`; **fixtures** - every fixture in `makefiles/` converts to exactly its committed `expected.yml` compared as a `ConfigSpec`, produces exactly its expected warning list, and reloads from its own serialized YAML to an equal `ConfigSpec`.

#### Phase 8: TUI and CLI surface
**Model:** opus

- [x] **OPEN** `app.rs:415,426,445` + `tui/mod.rs:18` - TUI terminal not restored on early-error paths, and no panic hook anywhere in the crate. `init_terminal` enables raw mode at `tui/mod.rs:18`; restore is only at `app.rs:445`; two `?` sit between them (`:415` `TaskScheduler::new(...).await?`, `:426` `terminal.draw(...)?`). `git grep "set_hook\|impl Drop" -- src/` returns nothing. Fix: standard ratatui panic hook + restore guard (Drop); every post-init error path flows through restore.
- [x] **OPEN** `tui/app.rs:85,149` + `app.rs:453` - Ctrl+C is dead while the TUI runs. `handle_key_event(&mut self, code: KeyCode)` receives only the code, modifiers dropped at the call site, and has no `CONTROL` arm. Post-quit, `scheduler_handle.await` blocks with no "waiting for N running tasks" message anywhere. Fix: handle `Char('c')` + CONTROL; print the waiting message wired to the Phase 2 cancellation path.
- [x] **CHANGED** `app.rs:314-325,376-389,344,361,264` - one of three fixed. **`is_virtual_parent` SHIPPED** (`121f384`): both paths now go through `impl From<parser::Task> for Task`, which carries the field at `executor/task.rs:249`, so it can no longer drift. **Still open:** no `build_executor_tasks` helper exists and the two setups remain parallel; and builtins are still undispatched in TUI mode, because `execute_with_tui` (`:344`) only calls `filter_execution_tasks` (`:361`) while the Clean/Graph/History/Stats dispatch lives solely in `execute_with_terminal_output` (`:295-313`). Under a TTY, `:264` routes `--tui Graph` into the TUI path, so it prints "No tasks to execute". No TUI/terminal parity test exists.
- [x] **OPEN** `parser.rs:407` - `--help` ignores `-o/--ottofile` and `$OTTOFILE`: `let ottofile_value = ".".to_string();`. Reproduced with `./otto.yml` (tasks `Convert2/build/fmt/test`) and `sub/other.yml` (task `ZZZuniquetask`): both `otto -o sub/other.yml --help` and `OTTOFILE=... otto --help` listed the wrong file's tasks, **while rendering `[env: OTTOFILE=.../sub/other.yml]` in the same output**, proving the value was read and then discarded. Fix: extract the flag/env value before divining. **STAYS: explicitly NOT taken by `2026-08-28`** (that bug lives in the ottofile divining here, not in the arg-declaration code its Phase 1 touched). Annotation verified true.
- [x] **SUPERSEDED, verified true** (`2026-08-28` Phase 1, commit `bb0f8dc`) the custom help omitting `-o/--ottofile`, `-C/--cwd`, `--list-subtasks`. `Self::global_args()` (`parser.rs:649`) now feeds `build_help_command` (`:1743`), `build_bare_help_command` (`:1789`), and `otto_command` (`:698`), with `build_help_command_with_error` (`:1794`) delegating to the bare builder. `otto --help` lists all of `-C/--cwd`, `-o/--ottofile`, `--list-subtasks`, `--tasks`, `--format`, `-j/--jobs`, `-t/--tui`, `--no-prefix`. Pinned by `test_help_global_flags_no_drift` (`:4004`) against `EXPECTED_GLOBAL_OPTIONS_HELP` (`:3986`). **That test is itself broken on CI: see Phase 0.**
- [x] **CHANGED** Builtins behind global flags silently no-op. **Narrower than the draft says:** `Clean`, `Graph`, `History`, and `Stats` work, because they have `find_tasks_by_name` handlers at `app.rs:296-313`. `Convert` and `Upgrade` have none, so `otto -j 2 Convert` and `otto -j 2 Upgrade` print "No tasks to execute". `handle_subcommand` (`main.rs:186`) only matches `args[1]`, so any leading global flag defeats routing regardless. Fix: route builtins in main by scanning past global flags; give app.rs handlers for all six.
- [x] **OPEN** `parser.rs:2463` - flag values colliding with task names break parsing. Reproduced: `otto build --msg test` gives `error: a value is required for '--msg <msg>' but none was supplied` because `test` was stolen as a task boundary; `otto build --msg=test` works. No lookahead in `partitions`, and **the error message does not name the `=` escape hatch**, so neither the fix nor the interim mitigation landed. **STAYS: explicitly NOT taken by `2026-08-28`** (its Phase 8 only documents the edge). Annotation verified true.
- [x] **CHANGED** CLI rule violations, each re-verified:
  - **OPEN** choices `ignore_case(true)`: `git grep ignore_case -- src/` returns nothing; `parser.rs:1698` uses a bare `PossibleValuesParser::new(choices)`. `otto fmt --format ASCII` gives `error: invalid value 'ASCII'` while `ascii` works.
  - **OPEN** `history --status` ValueEnum: `cli/commands/history.rs:61` is `Option<String>` and `:99-104` matches three literals with `_ => None`, so `--status FAILED` **and** `--status bogusvalue` both print the full unfiltered list.
  - ~~`-C=DIR` silently swallowed~~ **SUPERSEDED, verified true** (`2026-08-28` Phase 1, `bb0f8dc`): `main.rs:78` handles `arg.strip_prefix("-C=")`, the Arg stays registered in `global_args()` (`parser.rs:655`) since it is what renders help, and it is pinned at `main.rs:320-327`.
  - **OPEN** `--` separator: `main.rs:61` honors it but `partitions` (`parser.rs:2463`) has no `"--"` arm, so `otto build -- --msg=x` gives `unexpected argument '--msg=x'`.
  - **OPEN** `--log-level` / `xdg_data_dir()`: `otto --log-level debug` gives `unexpected argument`; `main.rs:33` still calls `dirs::data_local_dir()` and `:46` still reads `filter_or("RUST_LOG", "info")`.
- [x] **OPEN** TUI correctness batch, all four: width-0 wrap hangs (`tui/pane.rs:150` sets `max_width = inner_area.width as usize` with no `.max(1)`, and at 0 the remainder never shrinks at `:157-166`); the drain exits on broadcast Lagged (`:182`, `:247` both `while let Ok(...) = try_recv()`); the scroll model jumps bottom-to-top on the first Up (`scroll_offset: 0` at `:88`, `auto_scroll: true` at `:92`, `saturating_sub(1)` at `:266`) and the height is hardcoded `pane.scroll_down(20)` at `tui/app.rs:171`; dead `set_status` (`:100`), `Pane::id` (`:50`,`:176`), `Pane::status` (`:55`,`:262`) with zero call sites.
- [x] **CHANGED** Parser minors, each re-verified: repeated task on one command line silently drops the second arg set (`otto build --msg=a build --msg=b` ran once); `--tui` `.global(true)` (`parser.rs:690`) does not propagate into external subcommands, so `otto build --tui` gives `unexpected argument '--tui'`; the `--Serial` raw-string scan (`parser.rs:1443`) matches option values; `-o /nope/nothere.yml` gives a bare `No such file or directory (os error 2)` with no path context; `eprintln!("{e}")` at `main.rs:116,121,145,152,158` discards the eyre chain; `calculate_hash(&String)` at `parser.rs:66` and `executor/task.rs:257`, plus the ottofile default `"."` at `:662`. **Correction:** the draft's `:1048` unwrap is a stale line number; there is one production unwrap in parser.rs's pre-test region.
  - **NEW** `--no-prefix` suppresses the `[task]` prefix on task output but not on scheduler status lines: observed `OTHER` followed by `[other] finished successfully`. Cosmetic inconsistency in a v1.3.0 feature, not a silent-success defect.
- **Success criteria:** `otto -o <other> --help` lists the other file's tasks (and `OTTOFILE=<other> otto --help` likewise); `otto build --msg test` runs build with msg=test; `otto build -- --msg=x` reaches the task; `otto build --msg=a build --msg=b` is an error, not a silent single run; `otto -j 2 Convert` converts and `otto --tui Convert` converts identically; `--format ASCII` is accepted and the task sees `ascii`; `history --status bogus` errors; `otto build --tui` is accepted; `otto --log-level bogus` errors and `otto --log-level debug` runs; `otto -o /nope/nothere.yml` names the path; `--no-prefix` drops the prefix from scheduler status lines too; quitting the TUI restores the terminal (canonical mode and echo back on) and Ctrl+C inside the TUI cancels the run and names the tasks it is waiting on; `wrap_line` terminates at width 0 and the first Up scrolls one line rather than jumping to the top; a panic inside the TUI leaves the terminal usable.

#### Phase 9: Repo and dependency hygiene
**Model:** sonnet

- [x] **OPEN** Remove dead dependencies. Confirmed and landed: `tracing`, `tracing-subscriber`, and `tokio-test` deleted from `Cargo.toml` (had zero references in `src/`); `atty` replaced with `std::io::IsTerminal` at `app.rs` and `cli/parser.rs` (both call sites re-located since the doc's line numbers, as warned); `once_cell::sync::Lazy` replaced with `std::sync::LazyLock` at `cli/parser.rs`'s `DEFAULT_JOBS`; `tokio = ["full"]` trimmed to `["rt-multi-thread", "macros", "fs", "sync", "time", "signal", "io-util", "process"]`, verified by a green build and test run (not guessed). `levenshtein` confirmed wired at `cli/parser.rs:291` (unknown-task suggestion) and left alone per the Resolved Decision.
- [x] **CHANGED** Remove committed artifacts. `baseline-test-results.txt` and `baseline-verification-results.txt` deleted (referenced by nothing); `prior-art/doit` and `prior-art/make` deleted (absolute symlinks pointing outside the repo onto this machine's `~/repos` layout, so no relative-path fix could make them portable — they were personal dev-reference symlinks, not build inputs); `tests/README.md` (0 bytes) and `benches/README.md` (placeholder, no `[[bench]]` target registered in `Cargo.toml` and no other file in `benches/`) both deleted. `.gitignore`'s duplicate `/target/` entries collapsed to one; the global `*.png`/`*.svg` ignore is kept but now commented as intentional (`otto Graph`'s `GraphFormat::Png`/`Svg` output), not dropped, since no tracked `.png`/`.svg` exists today and the alternative (scoping the pattern to an output directory) isn't a decision this phase should make silently.
  - **Success criteria (added, none existed):** `git ls-files | grep -E 'baseline-|prior-art/(doit|make)|tests/README.md|benches/README.md'` returns nothing; `grep -c '^/target/?$' .gitignore` is 1, not 2.
- [x] **OPEN** Dead-code sweep (rust rule: never `#[allow(dead_code)]`). `cli/error.rs` deleted (confirmed zero external callers of any of its items via `grep`); `cfg/error.rs` does not exist in this checkout (doc drift — only `cli/error.rs` was ever present) and is not created; `utils.rs` deleted (`pub mod utils` removed from `lib.rs`, both functions dead); `executor/visualizer.rs` deleted (`OutputVisualizer` had zero callers outside its own tests); `workspace::verify_task` and its two tests deleted (zero callers outside tests). Dead colors family confirmed and removed: `get_task_color_with_context`, `colorize_task_prefix_with_context`, `colorize_task_name_with_context` (each zero-caller, verified individually — `get_task_color`/`get_task_color_combination` are NOT dead, they're called internally by `colorize_task_name`/`colorize_task_prefix`, so they stay). `ports/http.rs` deleted whole (Phase 5's retarget confirmed: `upgrade.rs` has its own independent `reqwest` client, zero external callers of `ReleaseFetcher`/`HttpReleaseFetcher`/`MockReleaseFetcher`). Re-verified `#[allow(dead_code)]` sites: only one remains (`upgrade.rs`'s `GitHubRelease.name` field, kept for deserializing the real GitHub API shape) — fixed by renaming to `_name` with `#[serde(rename = "name")]`, matching the sibling `_os`/`_arch` underscore convention already in that struct, so the attribute is gone entirely rather than counted as an accepted exception. `lint-unused` found 55 underscore bindings in 12 files (doc said 15, drifted): triaged each; the overwhelming majority are legitimate drop-guards (`_temp_dir`, `_db`, `_held`) or idiomatic discarded trait-impl parameters (`_path`/`_mode` no-ops, `_frame`/`_area`/`_focused` stub widgets) and are left as-is; two were dead residue with no guard purpose (`colors.rs`'s redundant re-derivation in `test_consistent_color_assignment`, `scheduler.rs`'s `_received_started`/`_received_finished` set-and-never-read flags) and were deleted.
  - **Success criteria (added, none existed):** `grep -rn 'allow(dead_code)' src/` returns nothing; `find src -name 'error.rs' -o -name 'visualizer.rs' -o -name 'utils.rs'` returns nothing; `grep -rn 'verify_task\|get_task_color_with_context\|colorize_.*_with_context' src/` returns nothing.
- [x] **CHANGED** File-size and test-placement sweep. **Re-verified against the live tree, not the doc's stale counts:** at the start of this phase `parser.rs` was 5037 lines, `scheduler.rs` 3518, `cfg/task.rs` 1874, `state/manager.rs` 2017 — all four over cap, all bigger than the doc's own numbers (the doc's figures predate Phases 1/3/6/8's growth). **44 files**, not 38, carried inline `#[cfg(test)] mod tests` (doc drift again). All 44 extracted to sibling `<stem>_tests.rs` files via `#[path = "..."] mod tests;` in the parent, with each sibling self-gated by an inner `#![cfg(test)]` rather than an outer attribute on the `mod` item — this is what satisfies the literal grep in the success criteria (an outer `#[cfg(test)]` line would still match it; the inner form does not, and still compiles the test module out of non-test builds). Two of those siblings (`cli/parser_tests.rs`, `executor/scheduler_tests.rs`) were themselves over 1500 lines purely from having absorbed their parent's bulk, so each was further split into `_a.rs`/`_b.rs` at a safe top-level-item boundary. `parser.rs` and `scheduler.rs` production code then decomposed via `include!` of sibling `impl Parser { ... }` / `impl<F: FileSystem + 'static> TaskScheduler<F> { ... }` fragment files (`cli/parser/{help,discovery,params,foreach,command,meta_tasks,config}.rs`, `executor/scheduler/{support,task_execution}.rs`) — **deviation from the doc's literal ask:** the doc says "decomposes along its natural seams" without naming a mechanism; `include!` was chosen over real submodules because these methods are private and a real submodule (`mod help;` etc.) would need every one of them widened to `pub(super)` to stay callable from the parent, whereas `include!` splices the fragment into the exact same module scope, so visibility is unaffected and the seam is invisible to every caller. `lib.rs:19`'s placeholder `it_works` 2+2 test deleted.
- [x] **OPEN** Production `.unwrap()` pass: the 8 `lock().unwrap()` sites in `ports/http.rs` are moot (file deleted, see above); `history.rs`'s and `stats.rs`'s timestamp `.unwrap()`s (re-located from the doc's stale `:281`/`:256`) now fall back to the epoch via `.single().unwrap_or_else(|| DateTime::<Local>::from(DateTime::<Utc>::MIN_UTC))`, the `Local`-timezone equivalent of `clean.rs`'s existing `Utc` pattern. Confirmed no `expect("lock poisoned")` anywhere in `src/`.
  - **Success criteria (added, none existed):** `grep -rn '\.unwrap()' src/cli/commands/history.rs src/cli/commands/stats.rs` shows no bare timestamp `.unwrap()`; `grep -rn 'lock().unwrap()' src/` returns nothing.
- [x] **CHANGED** Logging-rule sweep. Re-verified counts drifted further from the doc's already-corrected figures (expected, given the file moves above); did not re-tally them, since the actionable instruction was the entry-`debug!` placement, not the historical count. Added an entry `debug!` (name + the arguments most useful for reconstructing the call) to every function named on the diagnostic path: `cli::Parser::parse`, `parse_all_tasks`; `TaskScheduler::execute_all`, `execute_task`; `ActionProcessor::build_script`, `write_script`; `StateManager::record_run_start`, `record_run_complete`, `record_task_start`, `record_task_complete`, `record_task_skipped`, `delete_run`; `migrations::migrate`; `MakefileParser::parse` (entry plus an exit summary naming `targets` and `skipped_lines` with their line numbers, per the doc's explicit ask); `Workspace::init`. `ports::http`'s fetch/download bullet is moot (module deleted by this same phase, per Phase 5's retarget).
- [x] **OPEN** `ExecutionContext::new` silent fallbacks get warns: both `current_dir()` and the epoch-duration fallback now log a `warn!` naming the underlying error before substituting the default. `output.rs`'s double-create (`TaskStreams::new` creating `stdout.log`/`stderr.log`, then `process_output` immediately re-creating and truncating the same file) — confirmed nothing reads the file in that gap (checked call sites and the three existing tests), so the early create in `new()` was deleted rather than merely commented, removing the redundant I/O instead of just documenting it. `action.rs`'s `write_script` now returns `(PathBuf, String)` (path and hash) instead of just `PathBuf`, so `process()`'s three call sites use the returned hash instead of recomputing it. `TeeWriter`'s per-write flush now carries a comment naming the ordering guarantee it buys, per the doc's literal ask (a comment, not a behavior change).
  - **Success criteria (added, none existed):** `otto` run under a workspace whose `current_dir()` fails (simulated by a deleted cwd) logs a `warn!` naming the failure at `RUST_LOG=debug` rather than silently defaulting; `grep -n 'File::create' src/executor/output.rs` shows exactly one site (inside `process_output`), not two.
- **Success criteria:** `cargo tree` shows no `tracing`, `tracing-subscriber`, `atty`, `once_cell`, or `tokio-test` **as a direct dependency of `otto`** (`cargo tree --depth 1` has no such entry; `tracing` and `once_cell` still appear several levels down as transitive dependencies of `eyre`/`console`/`hyper-util`/`rustls`/`tempfile`/`serial_test`, which is out of this phase's control and not what "dead dependency" meant); `git grep -l '#\[cfg(test)\] *$' src/ | wc -l` returns 0; no file in `src/` exceeds 1500 lines; every function on the named diagnostic paths emits an entry `debug!`.

#### Phase 10: Doc truth
**Model:** sonnet

- [ ] **OPEN** Archive or delete the stale docs/ tail. `find docs -type f` returns **70 files and there is no `docs/archive/`**; `ls docs/*.md | wc -l` returns **39**, of which the allowlist in this phase's success criteria keeps **4** at top level, so **35 files move into a `docs/archive/` that must be created**. Stating the number because "archive or delete the stale tail" reads like a tidy-up and is in fact a bulk move. Every named file is still at top level: `cli2-readme.md`, `cli-two-pass-parser.md`, `clap-two-pass-parsing-design.md`, the data-passing trilogy, the ipc trio, `investigation-summary.md`, `flag-implementation-plan.md`, `tui-implementation-plan.md`, `testability-plan.md`, `upgrade-command-fix.md`, `sqlite-implementation-plan.md`. **v1.4.0 added to the tail rather than pruning it** (`docs/commands/ottofile-reference.md`, `docs/commands/ottofile-strict-schema-migration.md`, both legitimate living refs, correctly placed). Top-level docs/ keeps living references only.
- [ ] **CHANGED** `docs/makefile-converter-design.md`, all four sub-claims still false, **one with the direction reversed**: the headline still says `cat Makefile | otto convert > otto.yml` and `otto convert < Makefile` (`:9-11`) when the builtin is capitalized `Convert`; "meaningful error messages" (`:22`) and "`$@`, `$<`, `$^` ... will warn user" (`:38`) describe a warning path that does not exist; the fictional test-structure section is intact at `:301-420` and `tests/fixtures/` does not exist. **Correction: the `?=` precedence claim at `:150` is wrong in the doc, not the code.** The doc lists `` `:=`, `=`, `?=`, `+=` ``; `makefile/parser.rs:121` has the correct `[":=", "?=", "+=", "="]`. Fix the doc; the converter's separate `?=` handling bug stays in Phase 7.
- [ ] **CHANGED** `docs/design/2026-05-24-conditional-task-dependencies.md`. The when:always contradiction is intact (`:498` "never make a dependent unreachable" vs `:892`), and **the code sides with `:892`**: `scheduler.rs:176-178` returns Unreachable for a skipped source before matching on `when`. The `--skip` flag text at `:757` and `:892` stays, and there is no such flag (`grep '"skip"' src/cli/` is empty). Paired-edge validation (`:454`, `:855`) has zero implementation, so it cannot be "marked fixed". `skip_reasons` reaching the run record is only partly true (`skip_reason_for` at `scheduler.rs:299-320`, still not persisted). **DROPPED sub-bullet:** the draft says to correct "cycle detection still works" to say it now exists. **It does not exist.** Repo-wide the only cycle detection is file-dep circularity (`scheduler.rs:2085`), env circularity (`env.rs:81`), and a resolver recursion guard (`resolver.rs:136`). This line becomes true only after Phase 1 ships; until then the doc's statement stays flagged, not "corrected".
- [ ] **CHANGED** `docs/project-aware-stats-design.md`. `grep -rn "get_task_stats_for_project\|list_projects\|list-projects" src/` returns **zero hits**; the `stats` meta-task (`parser.rs:2072-2155`) injects only `task`, `limit`, `json`. (The `--project` at `parser.rs:2023` belongs to `history`, and `:1921` to `clean`.) **Framing correction:** the doc's checkboxes at `:376-426` are all unchecked; it is the **step headings** (`:375,384,392,400,409,414,422`) and a success-criteria list at `:500-506` that carry ✅. Decision unchanged: **implement** `--project` and `--list-projects`; fix the false "graceful degradation shows a warning" at `:370` via Phase 4's try_new warn; remove the timeline section at `:508`.
- [ ] **OPEN** `README.md:121` still claims "If the version in `Cargo.toml` does not match the latest tag, a warning will be printed at build time." `build.rs` is 23 lines: `git describe --tags --always`, set `GIT_DESCRIBE`, two `rerun-if-changed` lines. No comparison, no `cargo:warning=`. Decision unchanged: **delete the claim**. The paired "add a real usage section" is also open: the README grew 62 -> 121 lines but its headings stop at Installation / Task Parameters / Env Vars / Version Reporting / Release process, with no builtins, `-j`, or `--tui`.
- [ ] **CHANGED** Comment-truth batch, re-anchored: `upgrade.rs:463` "Atomic replace (rename is atomic on most filesystems)" unchanged and made true by Phase 5; **`cfg/env.rs:312` and `:332`** (not `:168`) carry `// ONLY use env_context - never fall back to system environment`, contradicted by `:21`; `executor/action.rs:180`'s doc comment "Only include variables that don't exist in task.values" contradicts its own body comment at `:184` "Include all environment variables"; `docs/design/2026-05-24-paramspec-roundtrip.md` has zero `shebang` mentions and needs the Phase 6 exception noted; two tests still end in `println` rather than assertions (`scheduler.rs:1943`, `:2122`). **Not verified:** the draft's "parser.rs help text for init-era behavior" was not isolated to a specific string; treat as unchecked rather than open.
- [ ] **CHANGED** `examples/interactive-demo/PROOF.md` and `.cursor/rules/*.mdc`. PROOF.md is **still ALL-CAPS and still unrenamed**; `5f127eb` fixed its `interactive: true` to `tty:` and its commit body records that it flagged but did not rewrite the doc's description of a since-removed PTY implementation. **`.cursor/rules/` is worse than "verify or drop":** 5 files, all inaccurate, and two are now actively harmful. `configuration-format.mdc:43` and `feature-capabilities.mdc:55` document a `timeout:` task key that has never existed and **now hard-errors under `deny_unknown_fields`**, so anyone following them writes an ottofile that refuses to load. Also: three dead `mdc:` links (`src/cli/parse.rs`, `examples/ex1/otto.yml`, `examples/ex3/otto.yaml`), `development-patterns.mdc:53` describing "nom parsing" (nom is not in Cargo.toml), and `feature-capabilities.mdc:29` claiming a `-T` global flag that does not exist.
- [ ] **NEW** `docs/commands/ottofile-reference.md:39-42` documents six `otto:` keys as working behavior when they have zero consumers outside `cfg/otto.rs`: `jobs`, `home`, `verbosity`, `name`, `version`, `about`. v1.4.0's `deny_unknown_fields` now guarantees they parse, which makes accepted-and-ignored look official. Resolve per API Design: wire them or delete them from schema and reference together, keeping the drift test green.
- **Success criteria:** `grep -rn "cat Makefile | otto convert" docs/` returns nothing; `grep -n "warning will be printed at build time" README.md` returns nothing; `ls docs/*.md` returns only the enumerated living-reference allowlist (`grammar.md`, `schedulers.md`, `migration-guide.md`, `directory-layout.md`, plus `docs/commands/` and `docs/design/`), with every other current top-level file moved under `docs/archive/`; every key named in a `.cursor/rules/*.mdc` file loads under `deny_unknown_fields` (checked by feeding each documented key through `ConfigSpec` in a test).

#### Phase 11: Test-gap closure
**Model:** sonnet

(Fixes in Phases 0-10 land with their own regression tests; this phase holds the independent gaps.)

**Closed since the draft**, by v1.3.0/v1.4.0 test files: `propagate_params` diamond/chain/choices (`tests/param_propagation_test.rs`, 17 tests, `8ab7b39`); the serial-foreach matrix cell (`tests/serial_foreach_test.rs` 10 tests, `121f384`, plus `tests/foreach_aggregation_test.rs` 5, `cc36ad1`); the whole cfg bullet, already annotated superseded (`tests/roundtrip.rs` 11 tests `9106460`, `tests/env_self_reference_test.rs` `c026a7d`, `tests/env_command_substitution_test.rs` `10eb334`).

**Partial:** RealFs/MemFs equivalence (`ports/fs.rs` has 28 tests, per-impl not equivalence); workspace roots (`tests/workspace_root_test.rs` has 1 test, and it covers cwd anchoring, a different concern); converter round-trip (`tests/makefile_converter_test.rs:99`, one happy-path Makefile).

**Inverted, not closed:** `when: always` on a skipped source is now pinned end-to-end by `tests/serial_foreach_test.rs::test_predecessor_skipped_by_conditional_edge_cascades_visibly`, **but it pins the opposite of this doc's decision**. Phase 2 changes that test. There is still no direct `classify_edge` unit test.

- [ ] **OPEN** Parser: unknown-task error; builtin-exclusion from `*` expansion; value/boundary collision + `=` escape; help-path ottofile honoring; `-j 0`; repeated task; `--`.
- [ ] **OPEN** Scheduler: cycle rejection; spawn-failure path; panic-in-task observed; foreach + file-deps warm-cache; paired-edge rejection; exit-code recording; >32-completion channel backpressure.
- [ ] **OPEN (split from a wrongly-closed bullet, panel finding 2026-08-29)** cfg test gaps. The original bullet was tagged SUPERSEDED and counted as closed, which was not ground truth: only two of its eight items shipped. **Shipped and genuinely superseded:** deny-unknown-fields negative tests (`2026-08-29`, `b5a86dc`) and nested `$()` cases (`10eb334`, `tests/env_command_substitution_test.rs:47,63,93`). **Still open and re-verified in code:** the Nargs malformed-input table (`src/cfg/param.rs:339` still computes `Self::Range(min - 1, max)`), `deserialize_script_string` unit tests for dedent and shebang-args, the divine rejection table, the serialization-determinism test, literal `$$`, and the circular-message case (`src/cfg/env.rs:307` still rebuilds its regex per call). These ride Phase 6 and Phase 3 and are counted open here.
- [ ] **OPEN** State/ports: real v1 migration + crash window + newer-schema refusal branch; two-process concurrency (same-second runs, ensure_project race, SQLITE_BUSY); delete_run filesystem branch (every test passes false today, exactly why the wrong-dirname bug shipped); corrupt args JSON / unknown status rows; RealFs/MemFs equivalence pins.
- [ ] **CHANGED** Action/output: injection payloads through both generators, **now including foreach items and dynamic choices values**; script-hash stability; non-UTF-8 through process_output; symlinked/worktree/fs-root workspace roots; **foreach item path-traversal rejection**.
- [ ] **CHANGED** Upgrade: **archive survives the download call** (the new Phase 5 critical); staged-rename install; checksum verification; adversarial backup filenames; malformed release JSON. The `HttpReleaseFetcher` non-2xx/timeout/malformed-JSON gap is **withdrawn**: that module is dead and Phase 5 deletes it. The equivalent tests attach to upgrade.rs's inline client.
- [ ] **OPEN** TUI: width-0 wrap, Lagged drain, scroll model, terminal-restore guard (unit-testable via the guard type); TUI/terminal parity for builtin dispatch.
- [ ] **OPEN** Converter: the negative-construct fixture suite from Phase 7 with expected-output comparisons and the semantic round-trip-through-ConfigSpec test.
- [ ] **NEW (2026-08-29, found while verifying Phase 4)** **The integration suite writes into the developer's real database.** A full `cargo test` with neither `OTTO_HOME` nor `OTTO_DB_PATH` set creates a project row per temp fixture, named after the `tempfile` directory (`.tmpXXXXXX`). Measured on the live `~/.otto/otto.db` on 2026-08-29: **1040 of 1264 project rows were `.tmp*` test artifacts**, carrying 597 runs and 977 tasks, every one created between `2026-08-29 07:13:05` and `2026-08-30 06:20:56` - i.e. entirely by this plan's own phase runs. Deleting them left **224** real projects, which finally matches the "~222 project directories" this document counts elsewhere.

  **Why it matters beyond tidiness:** it silently corrupts `stats`, `history`, and any retention decision made against that DB, and it made two separate defects harder to see during this plan - the `otto-<hash>` vs `<name>-<hash>` prefix split and the "1252 project rows for ~222 directories" anomaly both read as production bugs when they were substantially test residue. It is also self-inflicted risk: a suite that writes to the real store is one `Clean` test away from deleting real history.

  **Scope:** `tests/cleanup_integration_test.rs` is already fixed (Phase 4 made it pin `HOME` + `OTTO_HOME` and `.env_remove("OTTO_DB_PATH")` per spawn, and it now passes with `OTTO_DB_PATH` set to anything or unset). The gap is every *other* file under `tests/` that spawns the binary or constructs a `StateManager` without isolating the home. Phase 4 made this cheap to fix: `OTTO_HOME` alone is now sufficient isolation, verified by `~/.otto/otto.db` staying byte-identical by md5 across a full `otto ci` under a scratch home.

  **Fix:** a single shared test helper that every integration test uses to spawn the binary or build a store, which sets `OTTO_HOME` to the test's own temp dir and removes `OTTO_DB_PATH`. Then pin it: a test that runs the suite's own helper and asserts the resolved DB path is under the temp dir, so a future test that forgets cannot regress silently.

- [ ] **CHANGED** CI coverage gate. Confirmed still excluded: `.otto.yml:175-177` is `ci: before: [lint, check, test]`, while `cov` (`:54`) and `cov-report` (`:89`, with `--fail-under` at `:92`) are referenced by nothing, and `.github/workflows/` has zero coverage references. Wire it with a floor at the current measured coverage so it can only ratchet up. **Prerequisite:** Phase 0, since a floor measured against a red suite is meaningless.
- **Success criteria:** each `Observed` line in this document maps to a named test in the inventory table that Phase 11 adds to this doc as it goes, and each such test is verified to fail when its fix is reverted (revert, run the single test, expect red, restore); `otto ci`'s task list includes `cov-report`; the suite passes on a runner, not only on the developing machine. **and a full `cargo test` with neither `OTTO_HOME` nor `OTTO_DB_PATH` set leaves `~/.otto/otto.db` byte-identical by md5.**
- **Coverage floor:** deliberately left unset until Phase 0 lands. A floor measured against a suite that cannot pass on CI is not a floor. **Mechanism, specified (panel round 2):** the floor is a literal committed value in `.otto.yml:92` (`--fail-under: X`), not a dynamic or computed check, so that raising it is a reviewable diff and lowering it cannot happen silently. Phase 11 sets `X` to the first green-on-a-runner measurement, and every later change that raises it is its own commit. **Obligation on the extraction:** the Phase 11 doc, when extracted per Alternative 4, must name that measured number before implementation begins; leaving it unset there is not permitted.

### Rollout

Per-phase commits on main via the standard flow (`otto ci` -> commit -> `bump`). Phase 0 must land first and alone (it makes every later phase's gate honest). Phases 1-5 are the "stop the bleeding" tranche: silent-success, injection, data-loss, and self-upgrade bugs that mis-execute or destroy user state today. 6-8 ship behind them; 9-11 back-to-back.

**2026-08-29: Phase 0 is now urgent rather than merely first.** CI has been red continuously since 2026-03-02, and the most recent release added a sixth month to that streak plus a fifth failing test. Every phase below it is unverifiable until it lands.

Coordination notes:

1. ~~Phase 6's `deny_unknown_fields` is behavior-changing~~ **SUPERSEDED and shipped** (`2026-08-29`, `b5a86dc`), with the migration note at `b0ad2bb`. The predicted breakage was real and was handled as an operator step in that doc.
2. ~~Phase 6's kebab-case keys ship with serde aliases~~ **MOVED OUT** to its own doc, see Addendum. What remains true and unshipped: the schema is inconsistent (snake retention keys beside kebab `on-failure` and `choices-command`).
3. **Phase 4's schema change** (dropping UNIQUE(timestamp), adding the run-dir column) rides the migration mechanism being fixed in the same phase: land the transactional/idempotent migration runner first within the phase, then the new migration on top of it. No such migration exists yet. Snapshot `~/.otto/otto.db` before the first post-change run.
   **Downgrade posture, decided 2026-08-29 (panel round 2):** the new schema version is a **one-way migration and is not backwards compatible**. Dropping a UNIQUE constraint in SQLite requires a table rebuild, so an older binary meeting the new DB sees a `runs` table it can still read but whose constraint it would re-impose on rebuild. Rather than pretend to support downgrade, the schema version is bumped and **older binaries refuse the newer schema with a named error telling the user to upgrade**, reusing the newer-schema refusal branch this phase already has to test. This mirrors the `otto.api` posture v1.4.0 shipped for ottofiles: fail closed with an actionable message rather than silently mis-reading. History is disposable local state; a user who must downgrade deletes `~/.otto/otto.db` and loses history, which is stated in the release notes.
4. **Phase 1's unknown-task error** is behavior-changing for anyone scripting `otto maybe-task || true` patterns; the near-match suggestion text makes the failure self-explanatory. Still unshipped.
5. **NEW: Phase 5 changes user-visible upgrade behavior from "silently broken" to "works".** Anyone who has run `otto Upgrade` has an unexplained failure in their history; the release notes should say the command did not work before this lands.

## Acceptance Criteria

Executed against `main` at HEAD `5f127eb` on 2026-08-29. Each records what the command returns **today**, so the criterion is falsifiable and its starting state is not assumed.

Every command below was typed into a shell against HEAD `5f127eb` on 2026-08-29 and its real output recorded. Behavioral probes ran under a scratch `HOME` and `OTTO_HOME`.

**Correction, 2026-08-29 (found while verifying Phase 1):** the original wording of this paragraph claimed the developer's `~/.otto` "was never touched". That claim was false for the database half, and is withdrawn. `OTTO_HOME` does not isolate the DB: `default_db_path()` (`src/executor/state/db.rs:53-55`) reads only `OTTO_DB_PATH` and otherwise hardcodes `$HOME/.otto/otto.db`, so a probe's run directories land in the scratch tree while its rows are written into the real `~/.otto/otto.db`. Demonstrated: three probe runs under `OTTO_HOME=/tmp/p1check/.otto` left the scratch `otto.db` at 0 bytes and added a `p1check` project plus three `boom` task rows to the real database (since deleted, with the DB backed up first). Re-running the same probe with `OTTO_DB_PATH` also set left the real DB's task count unchanged at 4286 and wrote `boom|7|failed` to the scratch DB, confirming `OTTO_DB_PATH` is the isolating knob today.

**Consequence for anyone re-running these criteria:** set `OTTO_HOME` *and* `OTTO_DB_PATH`. The two-variable requirement is itself the defect Phase 4 closes (`Derive the DB path from resolve_otto_home()`); once that lands, `OTTO_HOME` alone is sufficient and this correction becomes historical.

- [ ] **The `CI` workflow concludes `success`, and a tag push with a failing `CI` produces no release.**
  `gh run view 33284438194 --json conclusion -q .conclusion`
  **Observed on main:** `failure` (`530 passed; 5 failed`).
  `grep -c 'workflow_run\|needs: *\[*CI' .github/workflows/release-and-publish.yml`
  **Observed on main:** `0`. Release is ungated; run `33284439253` (Release, success) and `33284438194` (CI, failure) are the same SHA.
- [ ] **`otto <unknown-task>` exits non-zero and names the unknown task.**
  `otto nonexistent; echo $?`
  **Observed on main:** `No tasks to execute`, exit `0`. Also `otto nonexistent build` printed `[build] built` / `finished successfully` at exit `0`, silently discarding the bad token.
- [ ] **A quote-bearing env value reaches the task as literal text and executes nothing.**
  ottofile env `PAYLOAD: 'x"; touch $MARK; echo "y'`, task `bash: echo "payload=$PAYLOAD"`
  **Observed on main:** printed `[hello] y` then `[hello] payload=x`, exit 0, and `$MARK` **was created**. Injection is live.
- [ ] **Six consecutive runs of one unchanged ottofile produce exactly one script-cache filename.**
  `for i in 1 2 3 4 5 6; do otto hashme; done; find "$OTTO_HOME" -path '*.cache*' -name '*.sh' -printf '%f\n' | sort -u | wc -l`
  **Observed on main:** `6` (`2c4156c1 2ea9c33a 4f9df428 765bc17a 8da43080 f9a57063`).
- [ ] **A finished run is marked finished and carries its real exit code.**
  After a task whose body is `exit 7`:
  `sqlite3 $OTTO_HOME/otto.db "select name, exit_code, status from tasks;"`
  **Observed on main:** `boom|1|failed`. The real code 7 is lost.
  `sqlite3 $OTTO_HOME/otto.db "select id, status, ended_at, duration_seconds from runs;"`
  **Observed on main:** `1|1|running||` , i.e. `status='running'` with `ended_at` and `duration_seconds` both NULL after the process exited.
  **Criterion corrected during this gate run:** an earlier draft of this line named a column `completed_at`, which does not exist. The column is `ended_at` (`select sql from sqlite_master where name='runs'`). Recorded because it is the exact defect this gate exists to catch: a criterion written from the design rather than the running system.
- [ ] **Concurrent runs both persist.**
  `select sql from sqlite_master where name='runs'`
  **Observed on main:** the schema carries `timestamp INTEGER NOT NULL UNIQUE`, so two runs in the same second cannot both exist. Confirms the Phase 4 bullet at the schema level.
- [ ] **`otto Upgrade` completes an install against a fixture release.**
  **Cannot run on main, and deliberately not attempted:** the archive is deleted before extraction (`upgrade.rs:335-352`), and running the real command would replace the developer's installed binary. Depends on Phase 5. Verified by inspection plus a type-level reproduction of the `TempDir` drop, not by invoking the builtin.
- [ ] **`git ls-files Cargo.lock` prints `Cargo.lock`, and `ls makefiles/` names no Tatari service.**
  **Observed on main:** `git ls-files Cargo.lock` printed nothing; `ls makefiles/` printed `auth-svc`, `devs`, `makefile-example`, `media-planning-service`, `pre-commit-hooks`.

## Resolved Decisions

- **2026-06-10:** `when: always` + skipped source = Satisfied (cleanup runs on any terminal state). **Reaffirmed 2026-08-29** after finding the code does the opposite at `scheduler.rs:176-178` and that a v1.3.0 test pins the current behavior. The decision is unchanged and the test gets inverted in Phase 2; it closes two bullets in one edit and makes `parser.rs:1505-1507`'s own comment true.
- **2026-06-10:** `nargs` gets wired to clap `num_args`; `dest` and `constant` are deleted. **Reaffirmed 2026-08-29**: all three still have zero readers.
- **2026-06-10:** `--strict` is implemented, not removed. Unchanged.
- **2026-06-10:** `levenshtein` is wired, not removed. Unchanged, still unwired.
- **2026-06-10:** stats `--project` and `--list-projects` are implemented. Unchanged, still zero code.
- **2026-06-10:** the README build-warning claim is deleted, not implemented. Unchanged.
- **2026-06-10:** the converter stays heuristic with diagnostics rather than gaining a make grammar. Unchanged.
- **2026-08-28 / 2026-08-29:** both docs ship ahead of this one and own five named bullets. **Verified 2026-08-29: all five landed as described.** The de-duplication protocol worked and is the model for the next doc that jumps this one.
- **2026-08-29:** the kebab-case key flip leaves this doc (Addendum), honoring `2026-08-29:153`.
- **2026-08-29:** the `jobs: 0` config-load validation sub-bullet is dropped as invalid; the inert-`otto:`-keys item replaces it.
- **2026-08-29:** Phase 5's HTTP work retargets from `ports/http.rs` to upgrade.rs's inline client; the dead port is deleted in Phase 9.
- **2026-08-29:** the Tatari fixtures are deleted now; any git-history rewrite is a separate, deliberate, announced act and is explicitly not part of Phase 0. Recorded with the facts that inform it (`forkCount: 0`, `stargazerCount: 0`, first published 2025-12-08).
- **2026-08-29, panel round 1 (Architect + Staff Engineer), accepted in full:** the Phase 0 scorecard row was wrong (both seats found it independently, matching the author's own correction); Phase 11's cfg bullet was wrongly counted closed and is split; inverting `classify_edge` alone does not fix warm-cache foreach, so Phase 2 now specifies the three-step ordering and a `SkipKind` x `when` contract table; the Release gate needed a named mechanism and is now a `workflow_call` reusable workflow; Alternative 4 was tightened so each phase must be extracted into its own small doc before implementation (**superseded later the same day by the author: the extraction requirement is removed and this doc is executed directly, see Alternative 4**); three acceptance criteria were not falsifiable and now name an allowlist, a test inventory, and a deferred coverage floor; non-code claims now carry their exact `gh`/`git` commands.
- **2026-08-29, panel round 1, pushed back with rationale:** the Architect flagged wiring `levenshtein` as feature creep to justify a dead dependency. Rejected: the causality is the reverse. Phase 1's unknown-task-error bullet requires near-match suggestions to make the new failure self-explanatory, which is what makes the error a usable replacement for today's silent exit 0. The dependency is justified by the fix, not the fix by the dependency. If Phase 1 were cut, `levenshtein` would be deleted with it.
- **2026-08-29, panel round 1 close-out.** Panel call: no mistagged bullets, no unsound decisions, across ~25 bullets re-verified by command including all five supersession annotations. One must-fix (`.gitignore` anchor was `:10`, actually `:7`) and two cheap wins (state that the docs/ allowlist moves 35 of 39 files; name a table-driven test for the nine `SkipKind` cells) are applied. The panel independently found a third arithmetic defect, that the New column summed to +5 against a stated +7, because two listed "new items" were not top-level bullets; the plan is 98 items, not 100. It also sided with this doc against a reviewer on two anchors (`ci.yml:37` is bare `cargo clippy -- -D warnings`, and the nargs site is `param.rs:343` not `:339`).
- **2026-08-29, both scope objections closed by the panel, no owner action required.** The Architect's "unrequested scope" findings on stats `--project`/`--list-projects` and on wiring `levenshtein` were dropped in synthesis as re-raising entries already in Resolved Decisions, with the doc judged self-consistent on `levenshtein`. Recorded so neither is re-proposed.
- **2026-08-29, panel round 2, accepted in full.** Four findings, all applied. (a) **Two cells of the `SkipKind` table were wrong**: marking `SerialPredecessor` and `Unreachable` as satisfying `when: failure` silently redefined `when: failure` from "the source's action failed" to "something upstream prevented this node running", contradicting `2026-05-24-conditional-task-dependencies.md:86` and the shipped assertions at `tests/foreach_aggregation_test.rs:204,260`. The whole `failure` column is now Unreachable, and the serial-foreach bullet still closes because a failed member aggregates the parent to Failed, not Skipped. (b) **Phase 2 has five sites, not one.** The worker's dependency double-check at `scheduler.rs:955-963` is a second gate on the same edge, `SerialGroups::classify` at `:533` is a third decision point, and `skipped_set` must widen from `HashSet<String>` to a provenance map, cascading to `mark_skipped`, `skip_reason_for`, `classify_gates`, the up-to-date skip write, and the post-task routing. `completed_set` and `failed_set` are unchanged. (c) **The release gate was placed too late**: `needs: checks` must sit on `build-linux`/`build-macos`, since artifacts are built and uploaded there before `create-release`. (d) Reusable-workflow wiring: caller `env` does not propagate, and called-workflow permissions can only be downgraded.
- **2026-08-29: `when: failure` means the source task ran and its action exited non-zero.** Restated explicitly because a draft of this doc drifted from it. Skipped sources of any provenance do not satisfy `when: failure`. Answers the Staff Engineer's round-2 hardest question. Settled on four citations including the in-code invariant at `scheduler.rs:770-773`, plus two arguments that make it unarguable: no Phase 2 success criterion required the disputed cells, and admitting them would fan failure-handlers across the entire downstream cone of any single failure. **The seats split on this and the Architect was wrong**, having called the table correct without running anything; recorded so its bare verdicts are weighted accordingly next round.
- **2026-08-29, both pushbacks accepted by both seats.** `levenshtein` wiring is justified by Phase 1's fix rather than the reverse; the stats `--project` scope objection was withdrawn as scope-preference with no concrete remediation risk, with the seat noting Phase 10 correctly sits after Phase 4 so it does not bake in today's broken project/run identity.
- **2026-08-29:** `Release` gets gated on `CI`. Without it every other phase's "otto ci gates this" claim is unenforceable, which is the mechanism that let a red suite run for six months.
- **2026-08-29, later the same day, author decision superseding the panel-round-1 revision of Alternative 4:** the per-phase-extraction requirement is removed. This doc is implemented directly, all twelve phases in one run, one commit per phase, `otto ci` green before each commit. The panel's concern (a 100-item plan executed as one undifferentiated blob) is answered by the per-phase commit discipline and the per-phase `Success criteria` already in the plan, not by twelve additional documents.

## Alternatives Considered

### Alternative 1: Fix only criticals, defer hygiene and doc truth
- **Description:** Ship Phases 0-5; batch the rest "as touched."
- **Pros:** Faster to the high-value fixes.
- **Cons:** The hygiene tail IS the disease vector here: a red CI trained the repo to ignore its gates, near-zero logging made every silent failure undiagnosable, and stale docs misdirect future work. "As touched" is how three months of red CI accumulated. **2026-08-29 evidence:** it is now six months, the doc tail grew rather than shrank, and stale `.cursor/rules` files now describe a key that hard-errors.
- **Why not chosen:** No-deferments is the operating rule; every finding gets a phase.

### Alternative 2: Rewrite the Makefile converter on a real parser before fixing it
- **Description:** Replace the line-oriented heuristic parser with a proper make grammar (or shell out to `make -pn`).
- **Pros:** Eliminates the whole corruption class structurally.
- **Cons:** Large new surface for a convenience feature; `make -pn` requires make installed and varies across GNU make versions.
- **Why not chosen:** Phase 7's preprocessing pass + diagnostics makes the converter honest. A grammar rewrite is justified only if converter usage grows; that is the revisit trigger.

### Alternative 3: Move history off SQLite to JSONL
- **Description:** Append-only JSONL per project, no schema, no migrations, no locks.
- **Pros:** Deletes the migration-brick and busy-timeout classes outright.
- **Cons:** stats/history queries become full scans; clean's retention logic reimplements indexing; loses the transactional task/run relationship; the SQLite layer is otherwise sound.
- **Why not chosen:** The defects are fixable at the connection/migration layer; the storage choice was not the problem.

### Alternative 4 (added 2026-08-29): Split this doc into per-phase docs and retire it
- **Description:** Let each phase become its own small doc on the `2026-08-28`/`2026-08-29` model, and delete this one.
- **Pros:** That model demonstrably ships. Both small docs went from draft to tagged release inside two days, while this one has been unbuilt for 2.5 months. Small docs also get real review attention per bullet.
- **Cons:** Loses the cross-phase view that makes the six systemic classes visible; the three `otto-<hash>` sites, the three home-dir mechanisms, and the injection sites across four generators are each one fix precisely because one document sees them all. Twelve separate docs would re-litigate ordering twelve times.
- **Why not chosen, as revised later on 2026-08-29 (author decision, superseding the panel-review revision earlier that day):** this doc is executed directly, phase by phase, as a single implementation run. The panel's earlier revision required each phase to be extracted into its own small doc before implementation; that requirement is removed. The cross-phase view is the reason this doc exists, the per-phase `**Model:**` tags and `Success criteria` already make each phase independently committable, and one commit per phase gives the same legibility an extracted doc would without re-litigating ordering twelve times.
- **Consequence for ship order:** Phase 0 lands first, alone, as its own commit, and the remaining phases follow in order in the same run.

## Technical Considerations

### Dependencies

One addition: `indexmap`, for deterministic author-ordered task/param maps. **It is currently a transitive dependency only** (via `petgraph` -> `daggy` and via `serde_yaml`), so this is a new *direct* dependency, added via `cargo add`. Removals: `tracing`, `tracing-subscriber`, `atty` (RUSTSEC-2021-0145, replaced by `std::io::IsTerminal`), `once_cell` (replaced by `std::sync::LazyLock`), `tokio-test`. `levenshtein` goes from dead to wired.

### Performance

Strictly improvements or neutral: deterministic script hashing makes the content-addressed cache actually hit; `spawn_blocking` for DB writes stops stalling the runtime; the stats N+1 collapses to grouped conditional aggregates; atomic writes add one rename per persisted file. **One measured regression fix promoted from "minor" to scope:** moving env.rs's per-call `Regex::new` to `LazyLock` statics, since deep chains are currently quadratic (12.1s at 100-deep, 22.3s at 150, 45.0s at 200).

### Security

Phases 3 and 5 are the security phases: script-generation injection closed for env values, params, **foreach items, and dynamic choices values**; foreach items rejected or slugified before becoming directory names, closing a path-traversal that currently writes outside the run's `tasks/` tree; command-substitution output no longer re-expanded, closing the parent-env leak; clean's symlink-following deletion fenced to canonicalized paths under the otto root in **both** the fs and DB paths; upgrade gains checksum verification against the per-tarball `.sha256` sibling and an atomic staged rename; the live HTTP client gains timeouts and status checks. Phase 0's removal of employer Makefiles from this confirmed-public repo is exposure cleanup, not a code fix, and lands first. Note the limit of that cleanup honestly: the files entered history on 2025-12-08 and the repo has been public throughout, so deletion stops future exposure and does not undo past exposure. The disclosed content is internal repo names, their Makefile bodies, and a developer's local path layout. No credentials or endpoints were found in them.

### Testing Strategy

Every fix in Phases 0-10 lands with a regression test in the same commit; TDD where the bug is empirically reproducible, which after the 2026-08-29 re-verification is nearly all of them: each `Observed` line above is a failing test waiting to be written. `otto ci` gates every phase by exit code; Phase 0 makes that gate real on CI as well as locally. **Two tests must be changed rather than added**: `tests/serial_foreach_test.rs:216-266` (Phase 2 inverts its assertion) and `test_help_global_flags_no_drift` (Phase 0 makes it runner-portable). Both are called out in their phases so neither looks like an undisclosed deviation during the implementation audit.

### Rollout Plan

See Rollout above. Releases follow the standard flow: phases land as commits on main, `bump` cuts the version after the tranche that closes each behavior-changing group (Phase 0 alone; Phases 1-5 together; the rest as they complete).

## Risks and Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Phase 0 stays unshipped and the rest lands on a red base | **High** | **High** | It has already happened twice (v1.3.0, v1.4.0 both shipped onto red CI). Phase 0 lands alone, first, and no later phase is called done until a runner is green |
| Typed scheduler channel refactor regresses drain-on-failure | Med | High | Drain-on-failure is empirically verified today; pin it with an integration test BEFORE the refactor and keep it green through |
| Inverting `classify_edge` for Always+Skipped changes shipped foreach behavior | Med | Med | One v1.3.0 test pins the current behavior and is inverted deliberately in Phase 2; the parallel-vs-serial asymmetry it currently encodes is itself the bug |
| Schema migration fails on an old DB | Low | High | Migration runner becomes transactional + idempotent in the same phase, with crash-window tests; snapshot otto.db before first run |
| Unknown-task error breaks `otto maybe-task` scripting patterns | Low | Low | Near-match suggestion text; release notes |
| IndexMap swap changes serialization output consumed by external tooling | Low | Low | Output becomes deterministic and author-ordered, strictly better; round-trip suite pins it |
| Upgrade staged-rename behaves differently on exotic filesystems | Low | Med | Stage beside the target (same mount); keep the existing backup mechanism as rollback |
| Converter warnings flood output on big Makefiles | Low | Low | Warnings are one line each with line numbers; `--strict` is opt-in |
| This doc ages another 2.5 months and drifts again | **High** | Med | Every bullet now carries a re-anchored `file:line` and an `Observed` line, so the next re-verification is mechanical rather than a fresh review |

## Open Questions

- [ ] None. All decisions are made above and recorded in Resolved Decisions.

## References

- Review source: eight-agent parallel review, 2026-06-10; re-verification: six-agent parallel pass, 2026-08-29, against HEAD `5f127eb`.
- `docs/design/2026-08-28-boundary-fixes-and-dynamic-foreach.md` (shipped v1.3.0; owns two bullets here, both verified)
- `docs/design/2026-08-29-strict-ottofile-schema.md` (shipped v1.4.0; owns three bullets here, all verified)
- `docs/design/2026-05-24-conditional-task-dependencies.md` (semantics contract for Phase 2)
- `docs/design/2026-05-24-paramspec-roundtrip.md` (round-trip invariant Phase 6 must preserve)
- `docs/design/2026-03-02-auto-pruning.md` (verified accurate)
- `docs/project-aware-stats-design.md` (unimplemented surface, Phase 10 decision)
- `docs/makefile-converter-design.md` (converter contract, Phases 7/10)
- `docs/commands/ottofile-reference.md` (published key inventory; Phase 10's six inert keys)
- **Missing:** `docs/design/2026-06-10-architecture-product-completion.md`, referenced by both shipped docs, absent from tree, history, and stash.
- `~/repos/.claude/rules/{general,cli,logging,rust,taste}.md` (the rules the drift sweeps enforce)

## Addendum: Parked and Removed Items

- **The kebab-case key flip** (removed from Phase 6, 2026-08-29). The retention keys (`keep_days`, `keep_last`, `keep_failed`, `auto_prune`, `prune_interval_hours`) are snake_case while `on-failure` and `choices-command` are kebab, which is a real inconsistency. It leaves this doc because `2026-08-29:153` already ruled it needs its own doc with aliases and a deprecation window, and because the surface grew: a flip now inverts a shipped test (`otto.rs:364`), rewrites the published 44-key reference, and must keep the drift test at `task.rs:1427` green. **Revisit condition:** the next time the ottofile schema is opened for any other reason.
- **`jobs: 0` validation at config load** (dropped 2026-08-29). `otto.jobs` has zero consumers, so validating it would guard nothing. Subsumed by the inert-`otto:`-keys decision.
- **`HttpReleaseFetcher` hardening** (withdrawn 2026-08-29). The module is dead; Phase 9 deletes it and Phase 5's hardening moves to the live inline client in upgrade.rs.
- **Splitting this doc into twelve** (rejected 2026-08-29, see Alternatives 4). Recorded so it is not re-proposed without the cross-phase argument being addressed.
- **Release-artifact signature verification** (parked 2026-08-29, raised by the Phase 5 implementer, uncovered by any phase). Phase 5 makes `otto Upgrade` verify a SHA-256 checksum against the per-tarball `.sha256` sibling before installing. **That makes the download tamper-evident, not trusted.** The sibling is served from the same host as the tarball, so anyone who controls the release host controls both, and `verify_binary` then *executes* the downloaded artifact before installing it. The checksum defends against a corrupted transfer, not against a compromised release host or a registry account takeover. Real defense is a detached signature (minisign or cosign) produced at release time and verified against a public key compiled into the binary. `git grep -in 'minisign|cosign|signature|sigstore'` over this document returns nothing: **no phase covers it.**
  **Why parked rather than added to Phase 5:** it is not a code fix in this repo's remediation surface. It requires a signing key and somewhere to keep it, a release-workflow step that signs every asset, a key-rotation story, and a decision about what a verification failure does to users running an unsigned older release. That is its own design doc, and folding it into a phase would repeat exactly the mistake Alternative 4 warns about.
  **Revisit condition:** before otto is recommended for installation outside this repo's authors, or the first time a release is cut from anywhere other than a maintainer's own GitHub Actions run. Recorded here so the checksum work is not mistaken for supply-chain integrity.
- **`--plan`, timeouts, lazy env evaluation, staged parser pipeline.** Never in this doc's scope; they belonged to the missing sibling doc. Recorded here so the scope is not silently absorbed and so the loss is visible.
