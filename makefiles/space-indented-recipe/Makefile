# The recipe below is indented with spaces instead of a tab. Make itself
# fails here with "missing separator"; the converter used to turn it into a
# task named `docker run --rm -v /tmp`.

build:
    docker run --rm -v /tmp:/tmp example
